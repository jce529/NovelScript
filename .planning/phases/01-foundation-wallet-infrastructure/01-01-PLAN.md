---
phase: 01-foundation-wallet-infrastructure
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [package.json, package-lock.json, vitest.config.ts, .env.example, .env.local]
autonomous: false
requirements: [AUTH-01]
user_setup:
  - service: supabase
    why: "Hosted Postgres + Auth backend for the whole phase (no local Docker/Supabase CLI available on this machine)"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> Data API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> Data API -> anon public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role key (secret)"
      - name: SUPABASE_DB_URL
        source: "Supabase Dashboard -> Project Settings -> Database -> Connection string -> Session mode (port 5432)"
    dashboard_config:
      - task: "Enable Google provider"
        location: "Supabase Dashboard -> Authentication -> Providers -> Google"
      - task: "Enable Kakao provider"
        location: "Supabase Dashboard -> Authentication -> Providers -> Kakao"
  - service: google-cloud-oauth
    why: "AUTH-01 Google social login"
    env_vars: []
    dashboard_config:
      - task: "Create OAuth 2.0 Web application Client ID, authorized redirect URI = https://<project-ref>.supabase.co/auth/v1/callback"
        location: "https://console.cloud.google.com/apis/credentials"
  - service: kakao-developers
    why: "AUTH-01 Kakao social login; D-02 requires email on every account, which needs Kakao Biz App approval"
    env_vars: []
    dashboard_config:
      - task: "Create app, enable Kakao Login, set Redirect URI = https://<project-ref>.supabase.co/auth/v1/callback, start Biz App conversion immediately (unknown review turnaround)"
        location: "https://developers.kakao.com/console/app"

must_haves:
  truths:
    - "A hosted Supabase project exists and is reachable, backing both Auth and Postgres for the rest of the phase"
    - "Google and Kakao are enabled as Supabase Auth sign-in providers"
    - "Local dev environment has real Supabase/OAuth credentials without any secret being committed to git"
    - "An automated test runner (Vitest) is installed and runs against this repo"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest configuration (node environment, tests/**/*.test.ts)"
      contains: "environment"
    - path: ".env.example"
      provides: "Documents the 4 required env vars with no real values"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
    - path: "package.json"
      provides: "Dependency manifest with Supabase/Vitest/Postgres packages"
      contains: "@supabase/ssr"
  key_links:
    - from: "package.json"
      to: "vitest.config.ts"
      via: "npm test script invokes vitest run"
      pattern: "vitest run"
---

<objective>
Stand up the external services and local dev/test tooling every later plan in this phase depends on: a hosted Supabase project (Postgres + Auth), Google + Kakao registered as Supabase Auth providers, and Vitest installed and runnable in this repo.

Purpose: Nothing else in Phase 1 (auth flows, wallet ledger, tests) can be built or verified without a real Supabase project and a working test runner — per 01-RESEARCH.md, this machine has no Docker/Supabase CLI/psql, so a hosted project is mandatory, and this is Wave 0 for that reason.
Output: A live Supabase project with Google + Kakao providers enabled, `.env.local` populated with real credentials (gitignored), `.env.example` documenting the required vars, and Vitest installed + configured (`npx vitest run` exits 0 with no test files yet).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-wallet-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-wallet-infrastructure/01-VALIDATION.md
@package.json
@.gitignore
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create Supabase project + register Google/Kakao OAuth apps</name>
  <files>none (external dashboard setup — no repo files touched by this task)</files>
  <read_first>
    - .planning/phases/01-foundation-wallet-infrastructure/01-RESEARCH.md (sections: "Environment Availability", "Pitfall 1: Kakao email requires Biz App conversion", "Pattern 6: Provider-agnostic OAuth architecture")
  </read_first>
  <action>
Claude cannot perform this task — it requires your login/identity on three external dashboards. Complete the following and report the 4 env values back before Task 2 runs.

1. **Supabase project** (hosted/free tier — no local Docker/CLI is available on this machine, per RESEARCH.md Environment Availability):
   - Go to https://supabase.com/dashboard/new, create a project (any name, e.g. `novelscript-dev`), pick a region near Korea if offered (Seoul, else Tokyo).
   - Project Settings -> Data API: copy **Project URL** and **anon public key**.
   - Project Settings -> API: copy the **service_role key** (secret — do not expose client-side).
   - Project Settings -> Database -> Connection string: select **Session mode (port 5432)** (NOT the 6543 transaction-mode pooler — RESEARCH.md Pitfall 3 explains why session mode is required for this project's raw SQL/migration and concurrency-test connections). Copy the full URI and substitute your actual DB password.

2. **Google OAuth Client** (https://console.cloud.google.com/apis/credentials):
   - Create OAuth 2.0 Client ID, Application type = "Web application".
   - Authorized redirect URI = `https://<project-ref>.supabase.co/auth/v1/callback` (project-ref = the subdomain in your Supabase Project URL from step 1).
   - Copy Client ID + Client Secret.
   - Supabase Dashboard -> Authentication -> Providers -> Google: paste both, toggle Enabled.

3. **Kakao Developers app** (https://developers.kakao.com/console/app):
   - Create an app. Under "카카오 로그인" (Kakao Login) enable it, set Redirect URI = the same Supabase callback URL as step 2.
   - 앱 설정 -> 요약 정보: copy the **REST API 키** (this becomes the Supabase Client ID).
   - 카카오 로그인 -> 보안: generate/copy the **Client Secret 코드**.
   - 앱 설정 -> 비즈 앱 전환: start Biz App conversion NOW. D-02 requires every account to have an email on file, and Kakao only exposes the `account_email` consent item to Biz Apps (RESEARCH.md Pitfall 1). Turnaround is unknown — do not block on approval, but do not skip starting it.
   - Do NOT enable Supabase's "Allow users without an email" toggle for the Kakao provider — this would violate D-02.
   - Supabase Dashboard -> Authentication -> Providers -> Kakao: paste REST API 키 as Client ID, Client Secret 코드 as Client Secret, toggle Enabled.

Report back (placeholders like `<pending>` are acceptable for anything not yet available, but Task 2 cannot proceed with a placeholder `SUPABASE_DB_URL` since the migration in Plan 01-02 needs a real connection):
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `SUPABASE_DB_URL`
  </action>
  <acceptance_criteria>
    - Supabase project shows "Active"/healthy status in the dashboard
    - Supabase Dashboard -> Authentication -> Providers shows Google = Enabled
    - Supabase Dashboard -> Authentication -> Providers shows Kakao = Enabled
    - Kakao Biz App conversion request has been submitted (approval pending is acceptable)
    - All 4 env values (or explicit `<pending>` placeholders) have been provided
  </acceptance_criteria>
  <verify>
    <automated>MISSING — external account/dashboard setup has no CLI/API surface available in this environment (no Supabase CLI, no gcloud/kakao API access configured); Task 2's automated verify (`npx vitest run` plus file checks on .env.local) is the closest downstream automated confirmation that these values were captured correctly</automated>
  </verify>
  <resume-signal>Reply with the 4 values (or explicit `&lt;pending&gt;` placeholders) to continue to Task 2</resume-signal>
  <done>Supabase project provisioned; Google and Kakao enabled as Supabase Auth providers; Kakao Biz App conversion requested; 4 credential values captured for Task 2</done>
</task>

<task type="auto">
  <name>Task 2: Install dependencies, configure Vitest, write env files</name>
  <files>package.json, package-lock.json, vitest.config.ts, .env.example, .env.local</files>
  <read_first>
    - package.json (current scaffold state — Next.js 16.3.2 / React 19.2.8, no deps beyond the scaffold)
    - .gitignore (confirms `.env*` is already ignored — do not add a duplicate entry)
  </read_first>
  <action>
Install exact versions verified in 01-RESEARCH.md's Standard Stack table:
```
npm install @supabase/supabase-js@2.112.4 @supabase/ssr@0.12.5 zod@4.4.3 drizzle-orm@0.45.2 postgres@3.4.9 pg@8.23.0
npm install -D vitest@4.1.11 @vitejs/plugin-react
```
(If any of these exact versions is no longer resolvable from the registry — packages were moving fast per RESEARCH.md — install the latest patch/minor within the same major version and note the actual installed version in the plan's SUMMARY.)

Create `vitest.config.ts` at the repo root:
```ts
import { defineConfig } from 'vitest/config';
import path from 'node:path';

export default defineConfig({
  test: {
    environment: 'node',
    include: ['tests/**/*.test.ts'],
    testTimeout: 30000,
    hookTimeout: 30000,
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, '.'),
    },
  },
});
```

Add to `package.json` `"scripts"`: `"test": "vitest run"`.

Create `.env.example` at the repo root with exactly these 4 keys, no values:
```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_DB_URL=
```

Create `.env.local` (already gitignored via the existing `.env*` rule) populated with the real values reported in Task 1. If Task 1 returned `<pending>` for any value, write an empty string for that key and note in the SUMMARY which values are still outstanding — Plan 01-02's schema migration and Plan 01-03's session tests cannot run without a real `SUPABASE_DB_URL` and `NEXT_PUBLIC_SUPABASE_*` pair, so flag this loudly if incomplete.

Do not create any files under `tests/` yet (that starts in Plan 01-02/01-03). Confirm `npx vitest run` exits 0 with zero test files — Vitest 4.x's default behavior for no matching test files is a clean pass; if it instead reports an error for zero tests found, add `passWithNoTests: true` under `test` in `vitest.config.ts`.
  </action>
  <acceptance_criteria>
    - `package.json` `dependencies` includes `@supabase/supabase-js`, `@supabase/ssr`, `zod`, `drizzle-orm`, `postgres`, `pg`
    - `package.json` `devDependencies` includes `vitest`
    - `package.json` `scripts.test` equals `"vitest run"`
    - `vitest.config.ts` exists and contains `environment: 'node'`
    - `.env.example` exists and contains all 4 keys: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_DB_URL`
    - `.env.local` exists locally and `git check-ignore .env.local` exits 0 (confirmed ignored, never staged)
    - `npx vitest run` exits with code 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>All Wave-0 dependencies installed, Vitest configured and passing with zero test files, `.env.example`/`.env.local` in place with real (or explicitly flagged pending) Supabase/OAuth credentials</done>
</task>

</tasks>

<verification>
`npx vitest run` exits 0. `git check-ignore .env.local` exits 0. `.env.example` contains the 4 documented keys. Supabase Dashboard confirms Google + Kakao providers Enabled.
</verification>

<success_criteria>
A live Supabase project backs Auth + Postgres for the rest of Phase 1; Google and Kakao are registered and enabled as sign-in providers (Kakao Biz App conversion in flight); local dev has real credentials without any secret committed; Vitest is installed and runnable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-wallet-infrastructure/01-01-SUMMARY.md`
</output>
