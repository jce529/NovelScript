---
phase: 08-provider-adapter-idempotent-debit
plan: 09
type: execute
wave: 5
depends_on: ["08-06", "08-07", "08-08"]
files_modified:
  - .planning/phases/08-provider-adapter-idempotent-debit/08-VALIDATION.md
autonomous: false
requirements: [PROV-01, COST-01]

must_haves:
  truths:
    - "A writer generates with live Gemini through the adapter exactly as before: @mention injection, 3 presets, 4 styles, reply/draft/proposal parsing, wasCapped (success criterion 1)"
    - "Double-click/Enter and lost-response resend debit the wallet once for one key (success criterion 2)"
    - "No runtime remote countTokens call occurs and estimated vs reported prompt tokens on Korean prose are recorded (success criterion 3)"
    - "A structured refusal shows the Korean refusal notice with token usage, balance and a collapsed reason — never provider English text as a creative result (success criterion 4)"
  artifacts:
    - path: ".planning/phases/08-provider-adapter-idempotent-debit/08-VALIDATION.md"
      provides: "Recorded automated + manual evidence and sign-off status"
      contains: "nyquist_compliant"
  key_links:
    - from: "browser AiPanel"
      to: "ledger_entries"
      via: "one ai_generation row per idempotency key observed after lifecycle checks"
      pattern: "ai_generation"
---

<objective>
Run the complete automated gate, prepare the dev server with fixtures, and have the user verify the UI-SPEC browser states plus a live Gemini regression, then record evidence in 08-VALIDATION.md.

Purpose: Mock-only sign-off is not allowed for UI, lifecycle and live-provider behavior (VALIDATION.md).
Output: Updated 08-VALIDATION.md with results; user approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-provider-adapter-idempotent-debit/08-UI-SPEC.md
@.planning/phases/08-provider-adapter-idempotent-debit/08-VALIDATION.md
@.planning/phases/08-provider-adapter-idempotent-debit/08-06-SUMMARY.md
@.planning/phases/08-provider-adapter-idempotent-debit/08-07-SUMMARY.md
@.planning/phases/08-provider-adapter-idempotent-debit/08-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated gate + dev server preparation</name>
  <files>.planning/phases/08-provider-adapter-idempotent-debit/08-VALIDATION.md</files>
  <read_first>
    - .planning/phases/08-provider-adapter-idempotent-debit/08-VALIDATION.md
    - .planning/phases/08-provider-adapter-idempotent-debit/08-06-SUMMARY.md (DB evidence or BLOCKED)
    - .env.example (AI_PROVIDER_FIXTURE)
  </read_first>
  <action>
1. Run and record exit status + test counts (no secrets, no URLs) in 08-VALIDATION.md under a new `## Execution Evidence` section:
   - `npx tsc --noEmit`
   - `npm test -- tests/ai/provider-errors.test.ts tests/ai/token-estimate.test.ts tests/ai/cost-estimate.test.ts tests/ai/prompt-composition.test.ts tests/ai/provider-gemini.test.ts tests/ai/provider-fixture.test.ts tests/ai/chat-idempotency.test.ts tests/ai/chat-refusal.test.ts tests/ai/chat-action.test.ts tests/ai/chat-request-lifecycle.test.ts tests/ai/ai-panel-notice.test.ts tests/admin/sanctions.test.ts`
   - `npm test -- tests/wallet/ledger.concurrency.test.ts tests/ai/chat.test.ts` (real DB; if unreachable record `BLOCKED` verbatim, never PASS)
   - `npm run build`
2. Verify no remote pre-count remains: `grep -rn "countTokens" lib app` empty — record.
3. Set each Per-Task Verification Map row's Status to `green` / `red` / `blocked` from the actual runs.
4. Start `npm run dev` in the background (default fixture unset = live Gemini, requires GEMINI_API_KEY in .env.local). Tell the user which fixture value to set per step (restart dev server after changing AI_PROVIDER_FIXTURE in .env.local).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "## Execution Evidence" .planning/phases/08-provider-adapter-idempotent-debit/08-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Execution Evidence" .planning/phases/08-provider-adapter-idempotent-debit/08-VALIDATION.md` = 1
    - `grep -c "| TBD |" .planning/phases/08-provider-adapter-idempotent-debit/08-VALIDATION.md` = 0
    - `grep -rn "countTokens" lib app` prints nothing
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Automated evidence recorded honestly; dev server running for verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies live regression and UI-SPEC browser states</name>
  <files>.planning/phases/08-provider-adapter-idempotent-debit/08-VALIDATION.md</files>
  <read_first>
    - .planning/phases/08-provider-adapter-idempotent-debit/08-UI-SPEC.md §States to Verify (browser)
    - .planning/phases/08-provider-adapter-idempotent-debit/08-VALIDATION.md §Manual-Only Verifications
  </read_first>
  <action>Present the steps below to the user, switch AI_PROVIDER_FIXTURE in .env.local and restart the dev server between steps when asked, then record each step result (pass/fail + observation, no secrets) in the Manual-Only Verifications table of 08-VALIDATION.md.</action>
  <verify>User types approved; 08-VALIDATION.md Manual-Only rows have results</verify>
  <acceptance_criteria>
    - User resume signal is "approved" or failing steps are recorded
    - 7 >= 5
  </acceptance_criteria>
  <done>Steps A-G observed and recorded.</done>
  <what-built>Gemini behind ProviderClient, stable per-send idempotency key with ledger precheck and settlement recovery, local token estimate, Korean refusal/processed/failure notices in the AI panel, balance refresh, dev fixtures.</what-built>
  <how-to-verify>
Open http://localhost:3000/studio → an owned test work → a chapter editor (AI 패널 on the right). Keep the header 보유 토큰 visible.

A. Live regression (AI_PROVIDER_FIXTURE unset, real GEMINI_API_KEY):
  1. Type `@` in the manuscript and mention a KB doc; send "이어서 한 문단 써줘" → AI bubble with 본문 초안, 본문에 삽입하기 works, header 보유 토큰 decreases.
  2. Switch AI 지시 프리셋 (초보자/중급자/자유형) and 문체 프리셋 once each and send → normal replies.
  3. Ask "새 인물 문서 하나 제안해줘" → 문서 제안 저장 toast unchanged. 다시 생성하기 and 거부하고 지우기 behave as before.
  4. Note the dev-server log: no `countTokens` request; if a debug print of estimated vs reported prompt tokens is not available, record promptTokenCount from one request in 08-VALIDATION.md manually (optional).
B. AI_PROVIDER_FIXTURE=slow: double-click 보내기 and press Enter repeatedly → one user bubble, one AI reply; header decreases once.
C. AI_PROVIDER_FIXTURE=refusal-input: send → no bubble; slot shows 제목 `안전 정책에 따라 답할 수 없는 요청이에요`, 본문 `표현을 바꿔 다시 시도해보세요.`, 메타 `2토큰 사용 · 보유 토큰 N` (neutral colors, ShieldAlert icon); input text restored and focused; 거절 사유 보기 collapsed → keyboard Enter expands `입력 차단 · SAFETY · 유해할 수 있는 내용으로 판단됐어요.`; header balance refreshed. Repeat with refusal-output → `생성 중 차단 · RECITATION · ...`.
D. AI_PROVIDER_FIXTURE=rate_limited / unavailable / config: each shows its own red-accent title (`지금 요청이 몰려 있어요. 1분 뒤 다시 시도해주세요.` / `AI 응답을 받지 못했어요. 잠시 후 다시 시도해주세요.` / `AI 기능에 문제가 생겼어요. 계속되면 문의해주세요.`), focus on 다시 시도, user bubble kept once; 다시 시도 does not duplicate the bubble; no status code/provider name shown; balance unchanged.
E. AI_PROVIDER_FIXTURE=drop-response: send → `AI 응답을 받지 못했어요...` + 다시 시도 (header may not update yet — thrown results do not refresh); click 다시 시도 → `이미 처리된 요청이에요` notice with `보유 토큰 N`, input restored; balance did not decrease a second time.
F. In DevTools set Network → Offline, then send → pending clears, unavailable notice appears, buttons usable.
G. 384px panel: expanded reason and long titles do not push the input row out; light + dark; 200% zoom.
H. (Optional, Claude runs on request) SQL: count `ledger_entries` where reference_type='ai_generation' for your user before/after B and E — +1 per send.
  </how-to-verify>
  <resume-signal>Type "approved", or list the failing step letters with what you saw</resume-signal>
</task>

</tasks>

<verification>
- User approval recorded; 08-VALIDATION.md Manual-Only rows marked with results; `nyquist_compliant: true` only if all automated rows green and DB rows green (not blocked)
</verification>

<success_criteria>
All four ROADMAP Phase 8 success criteria observed live or explicitly recorded as blocked with reason.
</success_criteria>

<output>
After completion, create `.planning/phases/08-provider-adapter-idempotent-debit/08-09-SUMMARY.md`
</output>
