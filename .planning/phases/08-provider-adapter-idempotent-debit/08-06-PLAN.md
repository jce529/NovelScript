---
phase: 08-provider-adapter-idempotent-debit
plan: 06
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - tests/wallet/ledger.concurrency.test.ts
  - tests/ai/chat.test.ts
autonomous: true
requirements: [COST-01]

must_haves:
  truths:
    - "On real PostgreSQL, concurrent apply_wallet_delta calls with the same (wallet, 'ai_generation', reference_id) produce exactly one ledger row and one balance change — including when the first call consumes the last balance"
    - "On the real database, replaying chat() with the same idempotencyKey calls the provider once and leaves exactly one ai_generation ledger row"
    - "Concurrent same-key chat() calls against the real database debit once (ledger count 1)"
    - "A zero-usage completion writes a delta-0 ai_generation row that then blocks replay; the same UUID on another wallet is unaffected"
    - "If the authorized DB environment is unavailable the plan records BLOCKED — it never reports mock/PGlite runs as real PostgreSQL evidence"
  artifacts:
    - path: "tests/wallet/ledger.concurrency.test.ts"
      provides: "SQL-level same-reference concurrency and zero-delta cases"
      contains: "ai_generation"
    - path: "tests/ai/chat.test.ts"
      provides: "chat() + real ledger replay/concurrency/zero-usage/refusal cases"
      contains: "already_processed"
  key_links:
    - from: "tests/ai/chat.test.ts"
      to: "ledger_entries"
      via: "count rows where wallet_id=owner and reference_type='ai_generation' and reference_id=KEY"
      pattern: "ledger_entries"
---

<objective>
Prove COST-01 against the actual Supabase PostgreSQL ledger (unique constraint + row lock), which the offline fake in 08-03 cannot prove.

Purpose: VALIDATION.md requires real DB evidence for concurrency and low-balance replay before sign-off; research flagged the RPC's balance-check-before-ON-CONFLICT ordering as the trap.
Output: extended ledger.concurrency and chat integration suites, run against the configured DB (or recorded as blocked).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-provider-adapter-idempotent-debit/08-RESEARCH.md
@.planning/phases/08-provider-adapter-idempotent-debit/08-03-SUMMARY.md
@supabase/migrations/0001_init.sql
@tests/wallet/ledger.concurrency.test.ts
@tests/ai/chat.test.ts
@tests/helpers/db.ts
@tests/helpers/mock-provider.ts

<interfaces>
tests/helpers/db.ts: `pgPool(max)` (postgres via SUPABASE_DB_URL), `adminClient()` (service role), `createTestUser()`, `deleteTestUser(id)`.
tests/helpers/mock-provider.ts (08-03): `createMockProvider({ generateContent?, estimateInputTokens? })`, `okResult(text?, inputTokens?, outputTokens?)`.
lib/ai/chat.ts (08-03): `chat(supabase, client, input with idempotencyKey)` → ChatResult with `status` 'completed' | 'refused' | 'already_processed' | 'failed'.
lib/ai/cost.ts: computeDebitAmount lite: ceil(prompt*0.00021 + candidates*0.00126).
STATE.md blocker: "SUPABASE_DB_URL unreachable (tenant/user not found)" — recheck; do not assume.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SQL-level same-reference concurrency on apply_wallet_delta</name>
  <files>tests/wallet/ledger.concurrency.test.ts</files>
  <read_first>
    - tests/wallet/ledger.concurrency.test.ts (entire file; reuse createWalletUser + sql pool)
    - supabase/migrations/0001_init.sql lines 20-70
    - tests/helpers/db.ts
  </read_first>
  <behavior>
    - 'debits once for 10 concurrent same-reference ai_generation debits': grant 100 (ref 'grant'), then Promise.allSettled of 10 × `apply_wallet_delta(w, -7, 'ai_generation', KEY, 'chapter:x')` → final balance 93; `count(*)` where reference_type='ai_generation' and reference_id=KEY is 1
    - 'last-balance race: loser fails or no-ops but never double-debits': grant 7, Promise.allSettled of 2 × delta -7 same KEY → balance 0; ledger count for KEY 1; every rejected outcome message contains 'insufficient balance'
    - 'zero delta records a reference and a replay is a no-op': grant 50, apply(-0 as 0, 'ai_generation', KEY0) returns 50; ledger row exists with delta 0; second apply(-5, same KEY0) returns 50 and balance stays 50
    - 'same reference on different wallets is independent': walletA, walletB each granted 20; apply(-5, 'ai_generation', SHARED) on both → both balances 15, one row each
  </behavior>
  <action>
Append a `describe('ai_generation idempotent debit (COST-01)', ...)` to tests/wallet/ledger.concurrency.test.ts using the existing `sql` pool and `createWalletUser()` so cleanup stays limited to created test identities (existing afterAll). KEYs via `crypto.randomUUID()`. Use `Promise.allSettled` for the race cases (the RPC raises for the loser in the last-balance case). Do not modify schema or the RPC; do not add a migration.
Run the suite. If it fails ONLY because the DB environment is unreachable (e.g. connection/tenant errors from SUPABASE_DB_URL), stop and record in the SUMMARY: `DB evidence: BLOCKED — <sanitized error class, no URL/credentials>`; do not mark these cases passed and do not substitute PGlite results as real-Postgres evidence.
  </action>
  <verify>
    <automated>npm test -- tests/wallet/ledger.concurrency.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ai_generation" tests/wallet/ledger.concurrency.test.ts` >= 4
    - `grep -c "Promise.allSettled" tests/wallet/ledger.concurrency.test.ts` >= 2
    - `git diff --name-only -- supabase/` prints nothing
    - `npm test -- tests/wallet/ledger.concurrency.test.ts` exits 0, OR the 08-06-SUMMARY.md contains the literal `DB evidence: BLOCKED`
  </acceptance_criteria>
  <done>Real-DB proof of single debit per reference, including the exhausted-balance race — or an explicit BLOCKED record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chat() replay / concurrency / zero-usage / refusal against the real ledger</name>
  <files>tests/ai/chat.test.ts</files>
  <read_first>
    - tests/ai/chat.test.ts (entire file after 08-03 migration)
    - lib/ai/chat.ts (08-03 result shapes)
    - tests/helpers/mock-provider.ts
    - lib/ai/chat-result.ts
  </read_first>
  <behavior>
    Add `describe('chat() idempotent debit against the real ledger (COST-01)')` reusing the file's beforeAll owner/work/chapter; each case creates a fresh user via createTestUser() with a +1000 test_grant and deletes it at the end.
    - replay: KEY; provider generateContent vi.fn → okResult('[REPLY]\n첫 응답', 100, 200); first chat → status 'completed', balance 999; second chat same KEY → status 'already_processed', remainingBalance 999; generateContent called 1 time; `select count` of ledger_entries (wallet_id=user, reference_type='ai_generation', reference_id=KEY) = 1
    - concurrent: provider awaits a barrier resolved after both calls started; Promise.all of two chat() with the same KEY → ledger count for KEY 1; final balance 999; each status ∈ {'completed','already_processed'}
    - zero usage: okResult('[REPLY]\nx', 0, 0) → status 'completed'; ledger row for KEY with delta 0 exists; replay same KEY → 'already_processed' and generateContent not called again
    - other wallet: user B with a row for KEY does not block user A's call with the same KEY (A status 'completed', A has its own row)
    - refusal: provider returns { text:'', finishReason:'refusal', refusal:{stage:'input', reasonCode:'SAFETY'}, usage:{inputTokens:5000, outputTokens:0, thoughtsTokens:null, reported:{input:true, output:false}} } → status 'refused', refusal.debitAmount = ceil(5000*0.00021) = 2, remainingBalance 998, ledger row delta -2, result has no 'reply' key
  </behavior>
  <action>
Implement the cases using `adminClient()` for both the session argument and ledger assertions (same as existing tests). Query ledger with `admin.from('ledger_entries').select('delta', { count: 'exact' }).eq('wallet_id', id).eq('reference_type', 'ai_generation').eq('reference_id', KEY)`. Barrier: `let release!: () => void; const gate = new Promise<void>((r) => { release = r; });` provider awaits `gate`; call `release()` on the next macrotask (`setTimeout(release, 50)`) after starting both calls. Same BLOCKED rule as Task 1 when the Supabase environment is unreachable.
  </action>
  <verify>
    <automated>npm test -- tests/ai/chat.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "already_processed" tests/ai/chat.test.ts` >= 3
    - `grep -c "reference_type', 'ai_generation'" tests/ai/chat.test.ts` >= 2
    - `grep -c "reasonCode: 'SAFETY'" tests/ai/chat.test.ts` >= 1
    - `npm test -- tests/ai/chat.test.ts` exits 0, OR 08-06-SUMMARY.md contains the literal `DB evidence: BLOCKED`
  </acceptance_criteria>
  <done>End-to-end single-debit behavior of chat() proven on the real ledger, or blocked status recorded honestly.</done>
</task>

</tasks>

<verification>
- `npm test -- tests/wallet/ledger.concurrency.test.ts tests/ai/chat.test.ts` (requires SUPABASE_DB_URL, NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY)
- No files under supabase/ changed
</verification>

<success_criteria>
Real PostgreSQL evidence for COST-01 exists, or the blocker is recorded without false-pass claims.
</success_criteria>

<output>
After completion, create `.planning/phases/08-provider-adapter-idempotent-debit/08-06-SUMMARY.md`
</output>
