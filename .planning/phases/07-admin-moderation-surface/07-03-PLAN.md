---
phase: 07-admin-moderation-surface
plan: "03"
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - "supabase/migrations/0008_sanction_enforcement.sql"
  - "lib/auth/write-access.ts"
  - "lib/chapters/actions.ts"
  - "lib/works/actions.ts"
  - "lib/kb/actions.ts"
  - "lib/auth/writer.ts"
  - "lib/reader/reports.ts"
  - "lib/reader/likes.ts"
  - "lib/reader/bookmarks.ts"
  - "lib/reader/subscriptions.ts"
  - "lib/reader/progress.ts"
  - "lib/reader/views.ts"
  - "lib/ai/chat.ts"
  - "lib/commerce/actions.ts"
  - "tests/admin/sanctions.test.ts"
  - "tests/admin/sanctions.database.test.ts"
  - "docs/admin-write-surfaces.md"
autonomous: true
requirements: ["ADMIN-03"]
must_haves:
  truths:
    - "D-06: History/cache cannot diverge through ordinary clients or concurrent sanctions."
    - "D-07: Suspended users retain reads and wallet access while user-initiated writes are denied."
    - "D-08: Timed suspension expires at DB time without cron; permanent suspension is distinct."
    - "D-09: A recipient can acknowledge warnings without clearing sanctions."
    - "D-10: Sanctioning an author never hides their already-published works."
  artifacts:
    - path: "supabase/migrations/0008_sanction_enforcement.sql"
      provides: "Suspension enforcement without losing purchased reading"
  key_links:
    - from: "lib/ai/chat.ts"
      to: "lib/auth/write-access.ts"
      via: "assertCanWrite before provider invocation and charge"
---

<objective>
Suspension enforcement without losing purchased reading.
Purpose: Deliver ADMIN-03 while preserving the locked moderation decisions and existing reader/writer/commerce behavior.
Output: supabase/migrations/0008_sanction_enforcement.sql, lib/auth/write-access.ts, lib/chapters/actions.ts, lib/works/actions.ts, lib/kb/actions.ts, lib/auth/writer.ts, lib/reader/reports.ts, lib/reader/likes.ts, lib/reader/bookmarks.ts, lib/reader/subscriptions.ts, lib/reader/progress.ts, lib/reader/views.ts, lib/ai/chat.ts, lib/commerce/actions.ts, tests/admin/sanctions.test.ts, tests/admin/sanctions.database.test.ts, docs/admin-write-surfaces.md.
</objective>

<execution_context>
@C:/Users/chang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/chang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-admin-moderation-surface/07-CONTEXT.md
@.planning/phases/07-admin-moderation-surface/07-RESEARCH.md
@.planning/phases/07-admin-moderation-surface/07-PATTERNS.md
@.planning/phases/07-admin-moderation-surface/07-UI-SPEC.md
@.planning/phases/07-admin-moderation-surface/07-VALIDATION.md
@.planning/phases/07-admin-moderation-surface/07-02-SUMMARY.md
</context>

<threat_model>
Trust boundaries: authenticated browser input to Server Actions; application to service-role DB access; ordinary DB role to privileged functions.
T-07-01 authorization forgery: require session-derived actor and fresh membership for each admin operation.
T-07-02 ownership bypass: protect moderation columns and validate direct role access.
T-07-03 body/charge leak: preserve SQL protected-content boundary and reject blinded purchases.
T-07-04 partial/duplicate state: use atomic operations, stable retry identity, row locking and real concurrency tests.
T-07-05 privacy/XSS: minimal safe DTOs and escaped text; no secrets or internal notes in public output.
T-07-06 expiry/cache drift: DB-time checks and one history-to-cache path.
Test the threats applicable to this plan in its named test files; final integration in 07-07 covers cross-plan boundaries. High-severity failures block completion.
</threat_model>

<tasks>
<task type="auto">
  <name>03-01: Enforce effective suspension in database writes</name>
  <files>supabase/migrations/0008_sanction_enforcement.sql, tests/admin/sanctions.database.test.ts</files>
  <read_first>supabase/migrations/0006_admin_foundation.sql, supabase/migrations/0007_admin_operations.sql, supabase/migrations/0001_init.sql, supabase/migrations/0002_studio.sql, supabase/migrations/0003_reader.sql, supabase/migrations/0004_kb_custom_folders.sql, supabase/migrations/0005_commerce.sql; every modified file if already present; 07-CONTEXT.md, 07-RESEARCH.md, 07-PATTERNS.md and 07-UI-SPEC.md in this phase directory</read_first>
  <action>Create effective write-permission function based on non-deleted profile plus cached explicit sanction kind and DB now(); expiry equal to now is allowed. Restrict INSERT/UPDATE/DELETE policies separately for works, chapters, KB nodes/folders, profile writer upgrades and reader mutations without blocking SELECT. Audit permissive FOR ALL policies that could OR around the new restriction. Guard both purchase RPCs and user-triggered definer write RPCs. Suppress progress/view mutations for suspended authenticated readers without making their content read fail. Keep narrow warning-recipient acknowledgement RPC, login/logout and existing account self-deletion path available as account-control exceptions; self-deletion must not mutate sanctions or financial history. Test direct API roles, all mutation categories, permanent/expired/timed cases, warning during suspension and unchanged existing published visibility.</action>
  <verify><automated>npx vitest run tests/admin/sanctions.database.test.ts --passWithNoTests=false</automated></verify>
  <acceptance_criteria>Direct authenticated writes fail during active suspension; entitled/free reading and wallet SELECT work; expiry requires no database cleanup job; owner edits cannot bypass via alternate policy.</acceptance_criteria>
  <done>Direct authenticated writes fail during active suspension; entitled/free reading and wallet SELECT work; expiry requires no database cleanup job; owner edits cannot bypass via alternate policy.</done>
</task>

<task type="auto">
  <name>03-02: Wire server guards and inventory every mutation</name>
  <files>lib/auth/write-access.ts, lib/chapters/actions.ts, lib/works/actions.ts, lib/kb/actions.ts, lib/auth/writer.ts, lib/reader/reports.ts, lib/reader/likes.ts, lib/reader/bookmarks.ts, lib/reader/subscriptions.ts, lib/reader/progress.ts, lib/reader/views.ts, lib/ai/chat.ts, lib/commerce/actions.ts, tests/admin/sanctions.test.ts, docs/admin-write-surfaces.md</files>
  <read_first>lib/auth/account.ts, lib/auth/writer.ts, lib/ai/chat.ts, app/account/actions.ts, app/write/start/actions.ts, app/works/[workId]/actions.ts, app/works/[workId]/chapters/[chapterId]/actions.ts, app/studio/[workId]/chapters/[chapterId]/actions.ts, lib/auth/write-access.ts and all modified domain files if present; every modified file if already present; 07-CONTEXT.md, 07-RESEARCH.md, 07-PATTERNS.md and 07-UI-SPEC.md in this phase directory</read_first>
  <action>Add reusable session-based assertCanWrite helper and safe denial result. Inventory all action wrappers and domain writes in the runbook; ensure each routes through a checked domain method or document and close bypasses. Cover chapter create/save/publish/unpublish/reorder, work create, KB create/rename/delete/save/folders, writer upgrade, reports/likes/bookmarks/subscriptions, purchase creation/payment and AI generation BEFORE provider work or wallet charge. Keep isAccountActive semantics unchanged. Return/no-op reader bookkeeping for suspended readers. For an AI request racing suspension, check before charge/persist as well and document how in-flight completion is handled without charging denied output. Tests prove blocked AI causes no provider invocation or charge, social un-toggle cannot bypass, and failed permission lookup fails writes closed while reads remain available.</action>
  <verify><automated>npx vitest run tests/admin/sanctions.test.ts tests/commerce/actions.test.ts --passWithNoTests=false</automated></verify>
  <acceptance_criteria>Runbook lists each mutation, enforcement location and negative test; suspended read routes do not fail because progress updates were refused. No uncovered service-role write bypass remains.</acceptance_criteria>
  <done>Runbook lists each mutation, enforcement location and negative test; suspended read routes do not fail because progress updates were refused. No uncovered service-role write bypass remains.</done>
</task>
</tasks>

<verification>
Run every task command with real assertions, then npx tsc --noEmit for TypeScript changes. DB skips are unverified and must be cleared by blocking plan 07-07. Read installed node_modules/next/dist/docs guides before writing Next.js code. Existing unrelated worktree edits are not part of this plan. Update graphify after code changes.
</verification>

<success_criteria>
All listed observable truths and task acceptance criteria hold. Required tests are nonempty; failed/skipped environment checks are reported honestly. Do not mark Phase 7 complete until 07-07 database and UI acceptance passes.
</success_criteria>

<output>
Create .planning/phases/07-admin-moderation-surface/07-03-SUMMARY.md with changed files, actual checks, commits and deviations.
</output>
