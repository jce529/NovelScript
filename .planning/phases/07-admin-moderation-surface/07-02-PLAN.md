---
phase: 07-admin-moderation-surface
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - "supabase/migrations/0007_admin_operations.sql"
  - "lib/admin/actions.ts"
  - "lib/admin/queries.ts"
  - "tests/admin/moderation.test.ts"
  - "tests/admin/operations.database.test.ts"
autonomous: true
requirements: ["ADMIN-01","ADMIN-02","ADMIN-03","ADMIN-04"]
must_haves:
  truths:
    - "D-04: Blind, unblind, warn, suspend and report outcomes are audited in their transaction."
    - "D-17: Detail supplies report content, target body and author report/action history."
    - "D-18: Duplicate reports group by target; only the operator-reviewed set resolves together."
    - "D-19: Action reasons are required, dismissal notes optional."
    - "D-20: Default queue is open, oldest first, with resolved/dismissed filters."
    - "D-15: Only an authorized administrator can clear blinding."
    - "D-16: Re-review requests have a distinct queue and explicit terminal outcome."
  artifacts:
    - path: "supabase/migrations/0007_admin_operations.sql"
      provides: "Transactional moderation and grouped queue services"
  key_links:
    - from: "lib/admin/actions.ts"
      to: "supabase/migrations/0007_admin_operations.sql"
      via: "checked RPC with actor, reviewed report IDs and idempotency key"
---

<objective>
Transactional moderation and grouped queue services.
Purpose: Deliver ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04 while preserving the locked moderation decisions and existing reader/writer/commerce behavior.
Output: supabase/migrations/0007_admin_operations.sql, lib/admin/actions.ts, lib/admin/queries.ts, tests/admin/moderation.test.ts, tests/admin/operations.database.test.ts.
</objective>

<execution_context>
@C:/Users/chang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/chang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-admin-moderation-surface/07-CONTEXT.md
@.planning/phases/07-admin-moderation-surface/07-RESEARCH.md
@.planning/phases/07-admin-moderation-surface/07-PATTERNS.md
@.planning/phases/07-admin-moderation-surface/07-UI-SPEC.md
@.planning/phases/07-admin-moderation-surface/07-VALIDATION.md
@.planning/phases/07-admin-moderation-surface/07-01-SUMMARY.md
</context>

<threat_model>
Trust boundaries: authenticated browser input to Server Actions; application to service-role DB access; ordinary DB role to privileged functions.
T-07-01 authorization forgery: require session-derived actor and fresh membership for each admin operation.
T-07-02 ownership bypass: protect moderation columns and validate direct role access.
T-07-03 body/charge leak: preserve SQL protected-content boundary and reject blinded purchases.
T-07-04 partial/duplicate state: use atomic operations, stable retry identity, row locking and real concurrency tests.
T-07-05 privacy/XSS: minimal safe DTOs and escaped text; no secrets or internal notes in public output.
T-07-06 expiry/cache drift: DB-time checks and one history-to-cache path.
Test the threats applicable to this plan in its named test files; final integration in 07-07 covers cross-plan boundaries. High-severity failures block completion.
</threat_model>

<tasks>
<task type="auto">
  <name>02-01: Implement atomic moderation RPCs</name>
  <files>supabase/migrations/0007_admin_operations.sql, tests/admin/operations.database.test.ts</files>
  <read_first>supabase/migrations/0006_admin_foundation.sql, supabase/migrations/0005_commerce.sql, tests/admin/foundation.database.test.ts; every modified file if already present; 07-CONTEXT.md, 07-RESEARCH.md, 07-PATTERNS.md and 07-UI-SPEC.md in this phase directory</read_first>
  <action>Add service-role-only moderation command RPCs with actor membership recheck, idempotency key, target/version, exact reviewed report IDs and note. Validate reviewed rows are open and match the target; reject stale state instead of consuming newly arrived reports. Lock actor membership, target user/content and report rows in documented deterministic order, matching commerce profile-before-wallet-before-content order when touching those entities. Apply blind/unblind, warn, timed/permanent suspension or resolution/dismissal with all audit and report fields in one transaction. Require nonblank reasons for operative actions; separate public warning/blind reason from internal resolution_note. Use the foundation sanction function, never dual application writes. Implement resolve-review maintained/unblinded states and audit them. Revoke PUBLIC/anon/authenticated execute, set safe search_path and qualify SQL names. Test rollback on audit failure, mismatched report IDs, idempotent retry and late report preservation.</action>
  <verify><automated>npx vitest run tests/admin/operations.database.test.ts --passWithNoTests=false</automated></verify>
  <acceptance_criteria>Failed audit rolls back target/status/history changes; duplicate operation does not repeat a sanction; late reports remain open; browser roles cannot execute operations.</acceptance_criteria>
  <done>Failed audit rolls back target/status/history changes; duplicate operation does not repeat a sanction; late reports remain open; browser roles cannot execute operations.</done>
</task>

<task type="auto">
  <name>02-02: Expose checked commands and complete target-group queries</name>
  <files>lib/admin/actions.ts, lib/admin/queries.ts, tests/admin/moderation.test.ts</files>
  <read_first>lib/admin/auth.ts, lib/admin/types.ts, lib/reader/reports.ts, lib/commerce/actions.ts, supabase/migrations/0007_admin_operations.sql; every modified file if already present; 07-CONTEXT.md, 07-RESEARCH.md, 07-PATTERNS.md and 07-UI-SPEC.md in this phase directory</read_first>
  <action>Add server-only guarded query/command APIs. listReportGroups defaults open, sorts min(created_at) then stable target ID and paginates 25 complete target groups. Use SQL aggregation or a bounded group query so one target never splits across pages; distinguish work and chapter targets. Detail accepts an anchor report ID and loads all reports for its target plus target body, author past reports/actions and reviewed IDs/version. Review queue is separate and oldest first. Zod-validate commands, derive actor from requireAdmin(), never expose service key or full DB rows to client. Return safe error codes including stale_target and validation failures. Test default/filter/grouping/page boundaries, details, null chapter targets and redaction.</action>
  <verify><automated>npx vitest run tests/admin/moderation.test.ts tests/admin/authorization.test.ts --passWithNoTests=false</automated></verify>
  <acceptance_criteria>Every exported operational function authorizes independently; queue has all ADMIN-01 fields and detail contains body/history; invalid input makes no mutation call.</acceptance_criteria>
  <done>Every exported operational function authorizes independently; queue has all ADMIN-01 fields and detail contains body/history; invalid input makes no mutation call.</done>
</task>
</tasks>

<verification>
Run every task command with real assertions, then npx tsc --noEmit for TypeScript changes. DB skips are unverified and must be cleared by blocking plan 07-07. Read installed node_modules/next/dist/docs guides before writing Next.js code. Existing unrelated worktree edits are not part of this plan. Update graphify after code changes.
</verification>

<success_criteria>
All listed observable truths and task acceptance criteria hold. Required tests are nonempty; failed/skipped environment checks are reported honestly. Do not mark Phase 7 complete until 07-07 database and UI acceptance passes.
</success_criteria>

<output>
Create .planning/phases/07-admin-moderation-surface/07-02-SUMMARY.md with changed files, actual checks, commits and deviations.
</output>
