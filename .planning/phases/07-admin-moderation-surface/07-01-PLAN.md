---
phase: 07-admin-moderation-surface
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "supabase/migrations/0006_admin_foundation.sql"
  - "lib/admin/auth.ts"
  - "lib/admin/types.ts"
  - "tests/admin/authorization.test.ts"
  - "tests/admin/foundation.database.test.ts"
  - "docs/admin-bootstrap.md"
autonomous: true
requirements: ["ADMIN-01","ADMIN-02","ADMIN-03"]
must_haves:
  truths:
    - "D-01: Administrator membership is separate from reader/writer identity."
    - "D-02: Operational access uses a checked server-only service-role path."
    - "D-03: Non-admin page requests receive 404 and forged actions cannot mutate data."
    - "D-04: All actions and membership changes have attributable immutable history."
    - "D-05: First administrator is provisioned by explicit seed/manual SQL, never public bootstrap."
    - "D-06: Sanction history is authoritative and the profile cache has one protected DB write path."
  artifacts:
    - path: "supabase/migrations/0006_admin_foundation.sql"
      provides: "Moderation schema and administrator trust boundary"
  key_links:
    - from: "lib/admin/auth.ts"
      to: "lib/supabase/admin.ts"
      via: "session identity and active admin_users membership before service-role access"
---

<objective>
Moderation schema and administrator trust boundary.
Purpose: Deliver ADMIN-01, ADMIN-02, ADMIN-03 while preserving the locked moderation decisions and existing reader/writer/commerce behavior.
Output: supabase/migrations/0006_admin_foundation.sql, lib/admin/auth.ts, lib/admin/types.ts, tests/admin/authorization.test.ts, tests/admin/foundation.database.test.ts, docs/admin-bootstrap.md.
</objective>

<execution_context>
@C:/Users/chang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/chang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-admin-moderation-surface/07-CONTEXT.md
@.planning/phases/07-admin-moderation-surface/07-RESEARCH.md
@.planning/phases/07-admin-moderation-surface/07-PATTERNS.md
@.planning/phases/07-admin-moderation-surface/07-UI-SPEC.md
@.planning/phases/07-admin-moderation-surface/07-VALIDATION.md

</context>

<threat_model>
Trust boundaries: authenticated browser input to Server Actions; application to service-role DB access; ordinary DB role to privileged functions.
T-07-01 authorization forgery: require session-derived actor and fresh membership for each admin operation.
T-07-02 ownership bypass: protect moderation columns and validate direct role access.
T-07-03 body/charge leak: preserve SQL protected-content boundary and reject blinded purchases.
T-07-04 partial/duplicate state: use atomic operations, stable retry identity, row locking and real concurrency tests.
T-07-05 privacy/XSS: minimal safe DTOs and escaped text; no secrets or internal notes in public output.
T-07-06 expiry/cache drift: DB-time checks and one history-to-cache path.
Test the threats applicable to this plan in its named test files; final integration in 07-07 covers cross-plan boundaries. High-severity failures block completion.
</threat_model>

<tasks>
<task type="auto">
  <name>01-01: Create protected moderation foundation</name>
  <files>supabase/migrations/0006_admin_foundation.sql, lib/admin/types.ts, tests/admin/foundation.database.test.ts</files>
  <read_first>supabase/migrations/0001_init.sql, supabase/migrations/0002_studio.sql, supabase/migrations/0003_reader.sql, supabase/migrations/0005_commerce.sql, tests/commerce/database.test.ts; every modified file if already present; 07-CONTEXT.md, 07-RESEARCH.md, 07-PATTERNS.md and 07-UI-SPEC.md in this phase directory</read_first>
  <action>Add admin_users with active membership and grant/revoke history, append-only admin_actions, user_sanctions, warning acknowledgements and moderation_review_requests. Use existing profile/work/chapter IDs; enforce chapter/work consistency and a partial unique open request index that handles NULL chapter IDs explicitly. Add admin_blinded and safe public reason to works/chapters; add explicit sanction kind and sanctioned_until cache to profiles. Keep is_published independent. Protect new INSERT/UPDATE columns from authenticated owner writes; revoke table-wide privileges before granting safe editable columns without losing existing editable fields. Keep reports reporter read policy and category CHECK. Create a single locked DB sanction mutation path that writes history and derives effective cache, with warnings unable to clear suspension. Define camelCase DTOs and action result types used by later plans. Add disposable DB tests for ownership, role grants and protected-column writes.</action>
  <verify><automated>npx vitest run tests/admin/foundation.database.test.ts --passWithNoTests=false</automated></verify>
  <acceptance_criteria>Real-role tests reject self-grant, cache/flag insert/update/upsert and cross-work request targets; existing profile/studio edit columns still work. Missing DB configuration is recorded as blocked verification.</acceptance_criteria>
  <done>Real-role tests reject self-grant, cache/flag insert/update/upsert and cross-work request targets; existing profile/studio edit columns still work. Missing DB configuration is recorded as blocked verification.</done>
</task>

<task type="auto">
  <name>01-02: Add administrator guard and explicit bootstrap runbook</name>
  <files>lib/admin/auth.ts, tests/admin/authorization.test.ts, docs/admin-bootstrap.md</files>
  <read_first>lib/supabase/server.ts, lib/supabase/admin.ts, lib/auth/account.ts, app/account/actions.ts, lib/admin/types.ts; every modified file if already present; 07-CONTEXT.md, 07-RESEARCH.md, 07-PATTERNS.md and 07-UI-SPEC.md in this phase directory</read_first>
  <action>Implement requireAdmin() from authenticated session identity, non-deleted profile and active membership, returning only trusted actor identity and server-only admin client. Expose page 404 behavior and safe action denial; recheck every invocation, not a long-lived authorization cache. Unit-test anonymous, reader, writer, admin-writer, revoked/deleted admin and DB failure. Document transactional parameterized grant/revoke SQL with audit entry, explicit existing UUID input and no default production identity. Seed supports a supplied identity only and skips safely when absent. Do not add a management route. Read relevant installed Next.js server-function/auth documentation before code edits.</action>
  <verify><automated>npx vitest run tests/admin/authorization.test.ts --passWithNoTests=false</automated></verify>
  <acceptance_criteria>Unauthenticated and non-admin calls never execute operational commands; admin writers retain writer identity. Bootstrap runbook has no public first-user path.</acceptance_criteria>
  <done>Unauthenticated and non-admin calls never execute operational commands; admin writers retain writer identity. Bootstrap runbook has no public first-user path.</done>
</task>
</tasks>

<verification>
Run every task command with real assertions, then npx tsc --noEmit for TypeScript changes. DB skips are unverified and must be cleared by blocking plan 07-07. Read installed node_modules/next/dist/docs guides before writing Next.js code. Existing unrelated worktree edits are not part of this plan. Update graphify after code changes.
</verification>

<success_criteria>
All listed observable truths and task acceptance criteria hold. Required tests are nonempty; failed/skipped environment checks are reported honestly. Do not mark Phase 7 complete until 07-07 database and UI acceptance passes.
</success_criteria>

<output>
Create .planning/phases/07-admin-moderation-surface/07-01-SUMMARY.md with changed files, actual checks, commits and deviations.
</output>
