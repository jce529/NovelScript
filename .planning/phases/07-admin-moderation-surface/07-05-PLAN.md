---
phase: 07-admin-moderation-surface
plan: "05"
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - "app/admin/layout.tsx"
  - "app/admin/page.tsx"
  - "app/admin/actions.ts"
  - "app/admin/reports/[reportId]/page.tsx"
  - "app/admin/reviews/[requestId]/page.tsx"
  - "app/admin/loading.tsx"
  - "app/admin/error.tsx"
  - "components/admin/report-queue.tsx"
  - "components/admin/moderation-form.tsx"
  - "tests/admin/action-boundary.test.ts"
autonomous: true
requirements: ["ADMIN-01","ADMIN-02","ADMIN-03","ADMIN-04"]
must_haves:
  truths:
    - "D-03: Non-administrators see 404 at admin page entry."
    - "D-16: Re-review requests have their own tab."
    - "D-17: Report, target body and author history are available on the detail screen."
    - "D-18: Group counts and complete reviewed-set actions match the service contract."
    - "D-19: Operative actions require a reason; dismissal does not."
    - "D-20: Open/oldest is the initial queue with explicit historical status selection."
  artifacts:
    - path: "app/admin/layout.tsx"
      provides: "Administrator queue, evidence detail and action UI"
  key_links:
    - from: "components/admin/moderation-form.tsx"
      to: "app/admin/actions.ts"
      via: "form submits target/version and checked command"
---

<objective>
Administrator queue, evidence detail and action UI.
Purpose: Deliver ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04 while preserving the locked moderation decisions and existing reader/writer/commerce behavior.
Output: app/admin/layout.tsx, app/admin/page.tsx, app/admin/actions.ts, app/admin/reports/[reportId]/page.tsx, app/admin/reviews/[requestId]/page.tsx, app/admin/loading.tsx, app/admin/error.tsx, components/admin/report-queue.tsx, components/admin/moderation-form.tsx, tests/admin/action-boundary.test.ts.
</objective>

<execution_context>
@C:/Users/chang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/chang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-admin-moderation-surface/07-CONTEXT.md
@.planning/phases/07-admin-moderation-surface/07-RESEARCH.md
@.planning/phases/07-admin-moderation-surface/07-PATTERNS.md
@.planning/phases/07-admin-moderation-surface/07-UI-SPEC.md
@.planning/phases/07-admin-moderation-surface/07-VALIDATION.md
@.planning/phases/07-admin-moderation-surface/07-02-SUMMARY.md
</context>

<threat_model>
Trust boundaries: authenticated browser input to Server Actions; application to service-role DB access; ordinary DB role to privileged functions.
T-07-01 authorization forgery: require session-derived actor and fresh membership for each admin operation.
T-07-02 ownership bypass: protect moderation columns and validate direct role access.
T-07-03 body/charge leak: preserve SQL protected-content boundary and reject blinded purchases.
T-07-04 partial/duplicate state: use atomic operations, stable retry identity, row locking and real concurrency tests.
T-07-05 privacy/XSS: minimal safe DTOs and escaped text; no secrets or internal notes in public output.
T-07-06 expiry/cache drift: DB-time checks and one history-to-cache path.
Test the threats applicable to this plan in its named test files; final integration in 07-07 covers cross-plan boundaries. High-severity failures block completion.
</threat_model>

<tasks>
<task type="auto">
  <name>05-01: Build guarded queue and evidence pages</name>
  <files>app/admin/layout.tsx, app/admin/page.tsx, app/admin/reports/[reportId]/page.tsx, app/admin/reviews/[requestId]/page.tsx, app/admin/loading.tsx, app/admin/error.tsx, components/admin/report-queue.tsx</files>
  <read_first>lib/admin/auth.ts, lib/admin/queries.ts, lib/admin/types.ts, app/studio/layout.tsx, components/ui/tabs.tsx, components/ui/select.tsx, .planning/phases/07-admin-moderation-surface/07-UI-SPEC.md; every modified file if already present; 07-CONTEXT.md, 07-RESEARCH.md, 07-PATTERNS.md and 07-UI-SPEC.md in this phase directory</read_first>
  <action>Read installed Next.js route/params/auth docs before implementing. Build /admin default open queue, 25-group pagination, oldest timestamp and stable filters; preserve return URL on detail. Render complete individual report details, target body as safe text and author past reports/actions. Render separate re-review tab/detail with the same target evidence. Guard each data loader and use 404 for non-admin page access. Implement empty/loading/retry/error states, narrow-screen labeled rows and desktop evidence/action split exactly as UI-SPEC. Keep history scoped to the target author, not a new global audit UI.</action>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>Routes render actual query DTOs, not fixtures; no target group is split by pagination; 390px layout has no document-wide overflow and reports/reviews have distinct selection state.</acceptance_criteria>
  <done>Routes render actual query DTOs, not fixtures; no target group is split by pagination; 390px layout has no document-wide overflow and reports/reviews have distinct selection state.</done>
</task>

<task type="auto">
  <name>05-02: Wire action forms to authenticated commands</name>
  <files>app/admin/actions.ts, components/admin/moderation-form.tsx, tests/admin/action-boundary.test.ts</files>
  <read_first>lib/admin/actions.ts, lib/admin/auth.ts, lib/admin/types.ts, components/ui/dialog.tsx, components/ui/textarea.tsx, components/ui/button.tsx, .planning/phases/07-admin-moderation-surface/07-UI-SPEC.md; every modified file if already present; 07-CONTEXT.md, 07-RESEARCH.md, 07-PATTERNS.md and 07-UI-SPEC.md in this phase directory</read_first>
  <action>Export server actions that independently authorize and call domain commands with server actor ID. Form submits exact reviewed IDs/version plus stable per-operation retry UUID, target and note; select warning/timed/permanent suspension distinctly. Require future ISO expiry for timed suspension, show timezone and keep permanent null distinct. Confirm blind/suspension with target/effect, preserve form on errors, disable repeated submit while pending, handle stale_target by refresh. Separate public reason from internal note. Include blind/unblind, warn/suspend, resolve/dismiss and review maintain/unblind commands. Revalidate queue, target detail and affected public/studio routes after successful writes. Test direct unauthorized action invocation, forged actor input, validation and exact command binding.</action>
  <verify><automated>npx vitest run tests/admin/action-boundary.test.ts --passWithNoTests=false</automated></verify>
  <acceptance_criteria>All form commands reach real checked services; denial has no side effects; pending/error/focus restoration matches UI-SPEC; user text never renders as raw HTML.</acceptance_criteria>
  <done>All form commands reach real checked services; denial has no side effects; pending/error/focus restoration matches UI-SPEC; user text never renders as raw HTML.</done>
</task>
</tasks>

<verification>
Run every task command with real assertions, then npx tsc --noEmit for TypeScript changes. DB skips are unverified and must be cleared by blocking plan 07-07. Read installed node_modules/next/dist/docs guides before writing Next.js code. Existing unrelated worktree edits are not part of this plan. Update graphify after code changes.
</verification>

<success_criteria>
All listed observable truths and task acceptance criteria hold. Required tests are nonempty; failed/skipped environment checks are reported honestly. Do not mark Phase 7 complete until 07-07 database and UI acceptance passes.
</success_criteria>

<output>
Create .planning/phases/07-admin-moderation-surface/07-05-SUMMARY.md with changed files, actual checks, commits and deviations.
</output>
