---
phase: 04.1-kb
verified: 2026-08-31T20:15:00Z
status: passed
score: 5/5 must-haves verified (all 5 plans)
---

# Phase 04.1: 사용자 정의 폴더 기능 (KB 커스텀 폴더 + 회차 폴더 트리) Verification Report

**Phase Goal:** Writers can create their own folders anywhere in the KB tree (not just documents), 회차(chapters) appear as a fixed folder in that same tree so growing KB content and episodes stay organized as works/accounts scale, and an account-level shared folder space lets cross-work content (e.g. a shared world-bible) be mentioned from any work.
**Verified:** 2026-08-31T20:15:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All 7 structural folders (회차 + template/인물/장소/사건/세력/아이템) are seeded locked for every new work, and Phase 2's lock-regression is fixed | ✓ VERIFIED | `supabase/migrations/0004_kb_custom_folders.sql` re-locks the 5 previously-unlocked categories and `create_work()` loops over all 7 with `is_locked=true` unconditionally; `tests/studio/schema-smoke.test.ts` asserts exactly 7 locked parent-less nodes and that renaming a locked 인물 folder now raises `locked_node_immutable` |
| 2 | Every pre-existing work is backfilled with a 회차 folder, no orphaned chapters | ✓ VERIFIED | Migration step 4 (`insert ... where not exists`) backfills 회차 for legacy works; `schema-smoke.test.ts`'s dedicated backfill test simulates a legacy work and proves the re-applied (idempotent) migration adds the locked folder |
| 3 | A generic KB file can never be created directly under a 회차-category folder; only chapters + sub-folders belong there | ✓ VERIFIED | `guard_chapter_folder_no_kb_files` trigger (migration step 6) rejects `node_type='file'` inserts under a 회차-category parent, allows `node_type='folder'`; proven in `tests/kb/chapter-folder-guard.test.ts` |
| 4 | Deleting a 회차 sub-folder never deletes/orphans its chapters — they move to the 회차 root | ✓ VERIFIED | `soft_delete_kb_node` nulls `chapters.folder_id` before soft-deleting the kb_nodes row (migration step 7); proven end-to-end in `tests/chapters/folder-grouping.test.ts` ("nulls out folder_id ... when their 회차 sub-folder is deleted") |
| 5 | Writer can create a new top-level folder directly under the work root or the 계정 공유 폴더 root | ✓ VERIFIED | `createFolder()` in `lib/kb/actions.ts` (parentId=null → category='custom'); `CreateRootFolderButton` renders at both root headers in `layout.tsx`, wired to `createFolderAction`; proven in `tests/kb/custom-folder-create.test.ts` |
| 6 | Writer can create a nested folder inside any existing folder, inheriting its parent's category server-side (never trusting the client) | ✓ VERIFIED | `createFolder()` looks up the parent row and inherits `category` verbatim, rejecting mismatched owner/scope/work_id; `KbTreeActions`'s "새 폴더 만들기" button opens `CreateFolderDialog` on every folder row; proven in `custom-folder-create.test.ts` (nested-inherit + cross-owner-rejection cases) |
| 7 | 계정 공유 폴더's nodes are queryable as a list distinct from the work's own KB nodes | ✓ VERIFIED | `getWorkKbNodes`/`getAccountSharedNodes` split (replacing merged `getKbTree`), each scoped by `scope='work'`/`scope='account_template'`; `layout.tsx` renders two independent `<KbTree>` trees; `tests/kb/tree-query.test.ts` proves disjoint, non-leaking results |
| 8 | A custom folder is never locked and can be freely renamed/deleted | ✓ VERIFIED | `createFolder()` always inserts `is_locked: false`; `custom-folder-create.test.ts`'s "never locked" test creates→renames→deletes successfully |
| 9 | Writer can create a chapter inside a specific 회차 sub-folder (or the 회차 root, default), and it's grouped there | ✓ VERIFIED | `createChapter` accepts `folderId`, validated by `assertChapterFolder`; `/chapters/new?folderId=` reads the search param and threads it through; "새 회차 만들기" button in `KbTreeActions` navigates with `?folderId=`; proven in `tests/chapters/folder-grouping.test.ts` |
| 10 | A chapter's folder_id can only point at a real 회차-category folder in the same work | ✓ VERIFIED | `assertChapterFolder` queries `kb_nodes` filtered on `work_id`+`category='회차'`+`node_type='folder'`; rejection returns the exact UI-SPEC error copy; proven by the "rejects a folderId that is not a 회차-category folder" test |
| 11 | The tree-rendering layer can compute, for any 회차-category folder id, exactly which chapters belong there | ✓ VERIFIED | `groupChaptersByFolder` (pure, `lib/kb/tree.ts`) proven correct across all 4 spec cases in `tests/kb/chapter-tree-merge.test.ts`; consumed directly in `layout.tsx` and rendered via `chaptersByFolderId` in `kb-tree.tsx` |
| 12 | Writer typing @ sees mention results from the work's own KB docs AND every account-shared document | ✓ VERIFIED | `searchMentionNodes` UNIONs work-scoped (5 categories + custom) and account-shared (any category) queries, both owner_id-scoped; proven in `tests/ai/mention-search.test.ts`'s cross-scope describe block; human-verified live in browser per orchestrator's checkpoint report |
| 13 | A mentioned account-shared document's content reaches the AI prompt, not silently dropped | ✓ VERIFIED | `getMentionedNodesContent` fixed to resolve both work-scoped and `scope='account_template'` ids; `lib/ai/chat.ts` calls it and feeds `mentionedDocs` into `assembleUserContent` for the actual prompt; proven in test + human-verified live (AI reply reflected shared doc content) |
| 14 | Mention direction stays one-way — no cross-work/cross-owner leakage | ✓ VERIFIED | Both `searchMentionNodes` queries and `getMentionedNodesContent` queries are `owner_id`-scoped; `mention-search.test.ts` explicitly asserts another owner's shared doc never appears |
| 15 | Mention rows never show the raw 'custom'/'template' category string | ✓ VERIFIED | `mentionTrailingText()` in `MentionAutocomplete.tsx` maps `scope==='account_template'` → "계정 공유", `category==='custom'` → "사용자 폴더", else raw category (only the 5 fixed KB categories, which are already writer-facing Korean labels) |
| 16 | KB sidebar visibly shows two separate sections (작품 폴더 / 계정 공유 폴더), each independently browsable | ✓ VERIFIED | `layout.tsx` renders the work title header + `<KbTree>` (workTree), a `<Separator>`, then "계정 공유 폴더" header + `<KbTree>` (accountTree); human-verified live in browser |
| 17 | Writer can click a 'new folder' button at either root header | ✓ VERIFIED | `CreateRootFolderButton` rendered next to both headers in `layout.tsx`, wired to `createFolderAction` with `parentId=null`; human-verified live |
| 18 | Writer can create a nested folder inside any existing folder row via "새 폴더 만들기" | ✓ VERIFIED | `KbTreeActions` renders the FolderPlus icon button for every `node_type==='folder'` row regardless of lock state, opening `CreateFolderDialog`; human-verified live |
| 19 | 회차 appears as a folder row with chapters as leaf rows beneath it, each linking to that chapter's editor; the 회차 row links to /chapters | ✓ VERIFIED | `kb-tree.tsx`'s `TreeRow` renders 회차 root as a `<Link href=".../chapters">`, chapter leaves via `ChapterLeafRow` linking to `.../chapters/{id}`; human-verified live |
| 20 | Any non-locked folder can be renamed/deleted, with 회차 sub-folder deletion warning that chapters move to the root | ✓ VERIFIED | `DeleteNodeDialog`'s `isChapterFolder` branch shows "이 폴더 안의 회차는 삭제되지 않고 회차 폴더 상위로 이동해요."; gated on `!node.is_locked` in `KbTreeActions`; human-verified live including all 7 locked folders correctly hiding rename/delete |

**Score:** 20/20 truths verified (aggregated must_haves.truths across all 5 plans)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/0004_kb_custom_folders.sql` | Category widening, lock fix, seed/backfill, chapters.folder_id, guard trigger, cascade-null | ✓ VERIFIED | Present, applied (proven by all schema-smoke/guard tests passing against live DB), contains all required strings (`kb_nodes_category_check`, `'회차'`, `'custom'`, `chapter_folder_no_kb_files`, `chapters set folder_id = null`) |
| `tests/studio/schema-smoke.test.ts` | 7-locked-folder + backfill assertions | ✓ VERIFIED | Contains `'회차'`, backfill test present, passing |
| `tests/kb/chapter-folder-guard.test.ts` | Guard trigger proof | ✓ VERIFIED | Exists, contains `chapter_folder_no_kb_files`, passing |
| `lib/kb/actions.ts` | `getWorkKbNodes`/`getAccountSharedNodes`/`createFolder` | ✓ VERIFIED | All three exported, `getKbTree` fully removed, matches plan spec exactly |
| `app/studio/[workId]/kb/[nodeId]/actions.ts` | `createFolderAction` | ✓ VERIFIED | Present, wraps `createFolder` with server-resolved ownerId, never trusts client category |
| `tests/kb/custom-folder-create.test.ts` | Root/nested creation, inheritance, rejection, rename/delete proof | ✓ VERIFIED | 6 integration tests, all passing |
| `lib/chapters/actions.ts` | `assertChapterFolder` + folder_id-aware createChapter/listChapters | ✓ VERIFIED | All present, matches plan exactly |
| `lib/kb/tree.ts` | `groupChaptersByFolder` pure function | ✓ VERIFIED | Present, matches 4-case spec, consumed by layout.tsx |
| `tests/chapters/folder-grouping.test.ts` | folder_id assignment/validation/cascade-null proof | ✓ VERIFIED | Present, passing |
| `lib/ai/mentions.ts` | Cross-scope searchMentionNodes + getMentionedNodesContent | ✓ VERIFIED | Both cross-scope-aware, `account_template` string present, wired into `lib/ai/chat.ts` |
| `tests/ai/mention-search.test.ts` | Cross-scope + one-directional proof | ✓ VERIFIED | Contains `scope`, new describe block passing alongside 6 pre-existing tests |
| `app/studio/[workId]/layout.tsx` | Two-section sidebar render | ✓ VERIFIED | Contains `계정 공유 폴더`, wired to `getWorkKbNodes`/`getAccountSharedNodes`/`listChapters`/`groupChaptersByFolder` |
| `components/studio/kb-tree.tsx` | Chapter leaves + 회차-root-as-Link + per-category gating | ✓ VERIFIED | Contains `chaptersByFolderId`, `ChapterLeafRow`; matches plan exactly |
| `components/studio/kb-node-dialogs.tsx` | `CreateFolderDialog` + `CreateRootFolderButton` + gating + delete copy | ✓ VERIFIED | Both exports present, exact chapter-folder delete warning string present |
| `components/studio/chapters-nav-link.tsx` | Deleted (superseded by 회차 tree row) | ✓ VERIFIED | Confirmed absent on disk |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `0004_kb_custom_folders.sql` | `kb_nodes` | `kb_nodes_guard_chapter_folder` trigger | ✓ WIRED | Trigger created and attached; proven by guard test |
| `0004_kb_custom_folders.sql` | `chapters.folder_id` | `soft_delete_kb_node` nulls folder_id before soft-delete | ✓ WIRED | Present in migration, proven by folder-grouping cascade test |
| `app/studio/[workId]/kb/[nodeId]/actions.ts` | `lib/kb/actions.ts` | `createFolderAction` → `createFolder(supabase, ...)` | ✓ WIRED | Confirmed exact call, server-resolved ownerId |
| `app/studio/[workId]/chapters/new/actions.ts` | `lib/chapters/actions.ts` | `submitCreateChapter` → `createChapter` → `assertChapterFolder` | ✓ WIRED | Confirmed |
| `lib/ai/mentions.ts` | `lib/ai/chat.ts` | `getMentionedNodesContent` resolves both scopes for `chat()`'s prompt composition | ✓ WIRED | Confirmed: `chat.ts:97` calls `getMentionedNodesContent`, result feeds `assembleUserContent` (the actual AI prompt) |
| `app/studio/[workId]/layout.tsx` | `components/studio/kb-tree.tsx` | Two `<KbTree>` renders sharing one `chaptersByFolderId` | ✓ WIRED | Confirmed both renders present, `chaptersByFolderId` computed once and passed to the work tree (`{}` for the account tree, correct since chapters never live there) |
| `components/studio/kb-node-dialogs.tsx` | `app/studio/[workId]/kb/[nodeId]/actions.ts` | `CreateFolderDialog`/`CreateRootFolderButton` → `createFolderAction` | ✓ WIRED | Confirmed |
| `app/studio/[workId]/chapters/[chapterId]/actions.ts` | `lib/ai/mentions.ts` | mention-search server action → `searchMentionNodes` | ✓ WIRED | Confirmed (`actions.ts:60`) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `layout.tsx` sidebar tree | `workTree`/`accountTree` | `getWorkKbNodes`/`getAccountSharedNodes` (live Supabase queries, owner/scope-filtered) | Yes — real DB rows | ✓ FLOWING |
| `layout.tsx` chapter leaves | `chaptersByFolderId` | `listChapters` (real DB query including `folder_id`) → `groupChaptersByFolder` (pure merge) | Yes | ✓ FLOWING |
| AI prompt mentioned-doc content | `mentionedDocs` | `getMentionedNodesContent` (real DB query, two-scope merge) → `assembleUserContent` | Yes — confirmed by human live-verify (AI reply reflected actual shared-doc content) | ✓ FLOWING |
| Mention autocomplete results | `results` (MentionCandidate[]) | `searchMentionNodesAction` → `searchMentionNodes` (real DB query) | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full phase-relevant test suite | `npx vitest run tests/kb tests/chapters tests/studio tests/ai/mention-search.test.ts` | 17 files / 74 tests passed, including idempotent re-run of the migration inside the backfill test | ✓ PASS |
| Type safety | `npx tsc --noEmit` | Exit 0, clean | ✓ PASS |
| Interactive UI flows (folder creation at both roots/nested rows, 회차 chapter-leaf rendering, folder deletion preserving chapters, all 7 locked folders gated, cross-scope mention search + AI prompt inclusion) | Live Chrome browser automation by orchestrator (human collaborator) | Both checkpoints (04.1-04 Task 3, 04.1-05 Task 3) reported fully approved | ✓ PASS (human-verified, taken as credible per task context) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| KB-03 | 04.1-01, 04.1-02, 04.1-05 | Writer can create new folders anywhere in the KB tree except the two root containers themselves, including brand-new top-level folders directly under a work or the account-shared space | ✓ SATISFIED | Schema widened (category CHECK includes 'custom'), `createFolder`/`createFolderAction` data layer, `CreateFolderDialog`/`CreateRootFolderButton` UI all present and wired; integration + human-verified |
| KB-04 | 04.1-02, 04.1-04, 04.1-05 | Account-level "계정 공유 폴더" root containing multiple top-level shared folders, every document inside mentionable one-directionally from any work | ✓ SATISFIED | `getAccountSharedNodes` split query, two-section sidebar UI, cross-scope `searchMentionNodes`/`getMentionedNodesContent` all present and wired; integration + human-verified (AI response reflected shared-doc content) |
| KB-05 | 04.1-01, 04.1-03, 04.1-05 | 회차 appears as a fixed, locked folder in each work's KB tree; writers can create sub-folders inside it; chapters remain a distinct, non-mentionable content type | ✓ SATISFIED | 회차 seeded/locked/backfilled in schema, `assertChapterFolder`/`groupChaptersByFolder` data layer, chapter-leaf tree UI with `/chapters` link, chapters confirmed NOT included in `searchMentionNodes`'s file-node queries (only `node_type='file'` kb_nodes rows are searched — chapters table is a separate, unqueried table) |

No orphaned requirements — all three IDs mapped in REQUIREMENTS.md to Phase 04.1 are claimed and satisfied across the 5 plans.

### Anti-Patterns Found

None. Scanned `lib/kb/actions.ts`, `lib/chapters/actions.ts`, `lib/kb/tree.ts`, `lib/ai/mentions.ts`, `components/studio/kb-tree.tsx`, `components/studio/kb-node-dialogs.tsx`, `app/studio/[workId]/layout.tsx` for TODO/FIXME/placeholder/stub markers, empty handlers, and hardcoded-empty data flowing to render — none found. All state variables rendered in the sidebar trace back to real Supabase queries (Level 4 trace above).

### Human Verification Required

None outstanding. Both `checkpoint:human-verify` gates in this phase (Plan 04.1-04 Task 3: cross-scope mention search; Plan 04.1-05 Task 3: full KB tree UI end-to-end) were already completed live in a real browser by the orchestrator and reported as fully approved, covering all visual/interactive aspects (two-section sidebar layout, folder creation at both roots and nested rows, 회차 chapter-leaf rendering and /chapters link, folder deletion preserving chapters with correct warning copy, all 7 locked structural folders correctly gating rename/delete, "계정 공유" mention label, AI chat correctly reflecting mentioned shared-document content).

### Gaps Summary

None. All 20 aggregated observable truths across the phase's 5 plans are verified against the actual codebase: schema migration applied and idempotent, data-layer functions match their specs exactly (server-derived category, cross-scope one-directional queries, cascade-safe deletes), UI components are wired end-to-end from Supabase queries through to rendered rows and Server Action mutations, and the full relevant test suite (74 tests) plus `tsc --noEmit` pass cleanly. The two mandatory human-verify checkpoints were completed live and approved. REQUIREMENTS.md's KB-03/KB-04/KB-05 are all satisfied with no orphaned or unclaimed requirement IDs for this phase.

---
*Verified: 2026-08-31T20:15:00Z*
*Verifier: Claude (gsd-verifier)*
