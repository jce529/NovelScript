---
phase: 04.1-kb
plan: 05
subsystem: ui
tags: [next.js, sidebar, kb-tree, dialogs, folders, chapters]

# Dependency graph
requires:
  - phase: 04.1-02
    provides: getWorkKbNodes/getAccountSharedNodes (split KB tree queries) + createFolder/createFolderAction (server-derived category)
  - phase: 04.1-03
    provides: lib/kb/tree.ts's groupChaptersByFolder + listChapters selecting folder_id + assertChapterFolder guard
provides:
  - Two-section studio sidebar (작품 폴더 / 계정 공유 폴더), each independently browsable and each with its own root-level "새 폴더 만들기" entry point
  - Folder creation at every folder row in the tree (CreateFolderDialog + CreateRootFolderButton), not just at document-creation points
  - 회차 folded directly into the work KB tree as a Link to /chapters, with existing chapters rendered as ChapterLeafRow leaves beneath it (and beneath any custom sub-folder inside it)
  - Chapter-sub-folder-specific delete warning copy (folder delete does not delete its chapters — they move to the 회차 root)
affects: []
# This is the final plan of Phase 04.1 — nothing else in the phase depends on it.

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Root-header folder-creation button (CreateRootFolderButton) as a standalone trigger for parentId=null creation, since neither KB root (작품 폴더 / 계정 공유 폴더) is a real kb_nodes row with its own tree row to attach per-row KbTreeActions to"
    - "Chapters stay a separate table (never become kb_nodes rows) but are merged into the tree purely at the UI layer via chaptersByFolderId, keyed by the 회차-category folder id (or the 회차 root id when folder_id is null)"

key-files:
  created: []
  modified:
    - components/studio/kb-node-dialogs.tsx
    - "app/studio/[workId]/layout.tsx"
    - components/studio/kb-tree.tsx
  deleted:
    - components/studio/chapters-nav-link.tsx

key-decisions:
  - "Kept CreateNodeDialog/RenameNodeDialog untouched, only extended KbTreeActions/DeleteNodeDialog and added two new exports (CreateFolderDialog, CreateRootFolderButton) — matches the plan's explicit read_first instruction to preserve those two dialogs exactly"
  - "회차 root row itself renders as a Link (not a folder click-to-expand target) per the UI-SPEC's Interaction Contract, fully replacing the standalone ChaptersNavLink that previously sat outside the tree"

patterns-established:
  - "KbTreeActions gates its icon-button row per node.category ('회차' gets 새 회차 만들기 instead of 하위 문서 추가; every folder gets 새 폴더 만들기; rename/delete gated on !is_locked) — single source of truth for the UI-SPEC's per-category Interaction Contract table"

requirements-completed: [KB-03, KB-04, KB-05]

# Metrics
duration: ~25min
completed: 2026-08-31
---

# Phase 04.1 Plan 05: KB tree UI wiring Summary

**Studio sidebar now renders two independently-browsable sections (작품 폴더 / 계정 공유 폴더) with folder-creation entry points at both roots and every folder row, and 회차 is folded directly into the work tree as a Link with its chapters rendered as leaf rows — closing out Phase 04.1's KB-03/KB-04/KB-05 scope end-to-end.**

## Performance

- **Duration:** ~25 min (worktree fast-forward sync + env setup + 2 auto tasks + human-verify checkpoint)
- **Started:** 2026-08-31T10:30:00Z (approx, worktree sync/env setup)
- **Completed:** 2026-08-31T10:53:00Z (approx, after checkpoint approval)
- **Tasks:** 3 (2 auto + 1 checkpoint:human-verify)
- **Files modified:** 3 modified, 1 deleted

## Accomplishments
- `CreateFolderDialog` + `CreateRootFolderButton` wired to `createFolderAction`, giving every folder row and both root headers a folder-creation entry point (previously folder creation for anything below the two roots didn't exist in the UI at all)
- `KbTreeActions` now gates its action row per the UI-SPEC's per-category Interaction Contract table: 회차 folders get "새 회차 만들기" instead of "하위 문서 추가"; every folder (including 회차 and custom sub-folders) gets "새 폴더 만들기"; rename/delete remain gated on `!is_locked`
- `"app/studio/[workId]/layout.tsx"` renders two distinct `<KbTree>` sections (작품 폴더 / 계정 공유 폴더) sourced from `getWorkKbNodes`/`getAccountSharedNodes` respectively, with a divider and an empty-state for 계정 공유 폴더 when no custom shared folders exist yet
- `components/studio/kb-tree.tsx`'s `TreeRow` now renders chapter leaves (`ChapterLeafRow`, BookOpen icon, muted when unpublished) beneath any 회차-category folder (root or custom sub-folder) via a `chaptersByFolderId` lookup, and the 회차 root row itself is a `Link` to `/studio/{workId}/chapters` instead of a static expandable row
- `components/studio/chapters-nav-link.tsx` deleted — its previous role (pinned, always-visible 회차 entry point) is now served by the 회차 tree row's Link
- Live-verified end-to-end in the browser (Chrome, against a real work): two-section sidebar, top-level folder creation at both roots, nested folder creation inside a locked category (인물), 회차 chapter leaves + Link to /chapters, "새 회차 만들기" from the 회차 row, custom sub-folder creation/chapter-creation/deletion inside 회차 with the correct chapter-preserving warning copy, and all 7 locked structural folders correctly hiding rename/delete

## Task Commits

Each task was committed atomically:

1. **Task 1: CreateFolderDialog + KbTreeActions gating + chapter-folder delete copy** - `beccdab` (feat)
2. **Task 2: Two-section layout render + kb-tree.tsx chapter-leaf rendering** - `325fce1` (feat)

Task 3 was a `checkpoint:human-verify` gate (no code changes) — approved by the user after live browser verification against workId `e33f73fb-c36f-402e-9f20-1622aa04a01f` ("버그 재현용 작품"), all 8 verification steps passing.

## Files Created/Modified
- `components/studio/kb-node-dialogs.tsx` - added `CreateFolderDialog`/`CreateRootFolderButton`, extended `KbTreeActions` per-category gating, extended `DeleteNodeDialog` with `isChapterFolder` branch copy
- `"app/studio/[workId]/layout.tsx"` - rewrote to render two independent KB tree sections plus the chapter-grouping data fetch/merge
- `components/studio/kb-tree.tsx` - extended `TreeRow` to accept/render chapter leaves via `chaptersByFolderId`, added `ChapterLeafRow`, gated the 회차 root as a `Link`
- `components/studio/chapters-nav-link.tsx` - deleted (superseded by the 회차 tree row)

## Decisions Made
- None beyond what the plan specified — implemented exactly as written in `04.1-05-PLAN.md`'s `<action>` blocks.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fast-forwarded stale worktree branch onto master before starting**
- **Found during:** Environment setup, before Task 1
- **Issue:** This worktree's branch (`worktree-agent-a6c9f766aa95d18f3`) was checked out at `d6c1a84`, missing all four prior 04.1 plans (04.1-01 through 04.1-04) and their merged commits, plus the entire Phase 3 reader-loop work already on `master`.
- **Fix:** `git merge master --ff-only` (clean fast-forward, no conflicts — the worktree had zero local commits beyond the stale point)
- **Files modified:** none beyond the merge itself (141 files brought in, matching `master` exactly)
- **Verification:** `git log --all --oneline | grep 04.1` confirmed `04.1-04`'s merge commit was present afterward
- **Committed in:** N/A (fast-forward, no new commit)

**2. [Rule 3 - Blocking] Missing local dev environment (node_modules, .env.local)**
- **Found during:** Environment setup
- **Issue:** Fresh worktree had no `node_modules` and no `.env.local`
- **Fix:** `npm install`; copied `.env.local` from the main checkout (`C:\Users\chang\novelscript-mvp\.env.local`, gitignored, not committed)
- **Files modified:** none tracked (both gitignored)
- **Committed in:** N/A

**3. [Rule 3 - Blocking] `npx tsc --noEmit` failed on unrelated `app/layout.tsx` due to missing generated Next.js route types**
- **Found during:** Task 1 acceptance-criteria verification
- **Issue:** `LayoutProps<"/">` (Next.js 16's auto-generated route-prop type) doesn't exist until `.next/types` is generated; this fresh worktree had never run `next dev`/`next build`
- **Fix:** Ran `npx next typegen` once to regenerate `.next/types` (gitignored, not committed) — same fix as documented in Plan 04.1-04's SUMMARY for the same environment class
- **Files modified:** none tracked (`.next/` is gitignored)
- **Verification:** `npx tsc --noEmit` exits 0 afterward
- **Committed in:** N/A

**4. [Rule 3 - Blocking] Port 3000 occupied when starting the dev server for the checkpoint**
- **Found during:** Pre-checkpoint automation for Task 3's manual verification
- **Issue:** `npm run dev` reported port 3000 in use by another process (a sibling worktree's dev server)
- **Fix:** Let Next.js auto-select the next available port; it started on 3001
- **Files modified:** none
- **Verification:** `curl -s -o /dev/null -w "%{http_code}" http://localhost:3001/studio` returned `307` (redirect to `/login`, expected — server healthy); user then completed live verification against this server
- **Committed in:** N/A

---

**Total deviations:** 4 auto-fixed, all Rule 3 (blocking/environment setup), zero touching plan-scoped application logic.
**Impact on plan:** All four were prerequisites for being able to execute/verify the plan at all in a fresh, stale worktree running alongside sibling agents. No scope creep into application code beyond what the plan specified.

## Issues Encountered
None beyond the environment-setup deviations above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 04.1's full scope (KB-03 custom folders anywhere below either root, KB-04 explicit 계정 공유 폴더 section with cross-scope mentions, KB-05 회차 folded into the tree with chapter grouping) is live, wired, and visually verified end-to-end in the browser.
- This is the last plan in Phase 04.1 — no further plans depend on it within this phase.
- No blockers for phase completion/transition.

---
*Phase: 04.1-kb*
*Completed: 2026-08-31*

## Self-Check: PASSED

- FOUND: components/studio/kb-node-dialogs.tsx
- FOUND: "app/studio/[workId]/layout.tsx"
- FOUND: components/studio/kb-tree.tsx
- FOUND: .planning/phases/04.1-kb/04.1-05-SUMMARY.md
- CONFIRMED DELETED: components/studio/chapters-nav-link.tsx
- FOUND commit: beccdab
- FOUND commit: 325fce1
