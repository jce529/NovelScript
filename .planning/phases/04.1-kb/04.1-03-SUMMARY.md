---
phase: 04.1-kb
plan: 03
subsystem: api
tags: [supabase, zod, chapters, kb-nodes, vitest]

# Dependency graph
requires:
  - phase: 04.1-kb (plan 01)
    provides: "chapters.folder_id column, kb_nodes.category widened to include '회차'/'custom', chapter-folder guard trigger, soft_delete_kb_node cascade-null"
provides:
  - "assertChapterFolder server-side guard (D-05) validating a chapter's folder_id references a real 회차-category kb_nodes folder in the same work"
  - "createChapter/listChapters extended with folder_id (nullable = 회차 root)"
  - "groupChaptersByFolder pure helper (lib/kb/tree.ts) bucketing a flat chapter list by folder id for tree-rendering"
  - "/studio/[workId]/chapters/new accepts an optional folderId search param, threaded through to createChapter"
affects: [04.1-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Cross-table FK-style validation done in application code (assertChapterFolder), not a Postgres CHECK constraint, since CHECK can't reference another table"
    - "UI-layer merge (groupChaptersByFolder) keeps chapters and kb_nodes as separate tables/queries — folder grouping happens in a pure function, not a SQL join baked into the tree query"

key-files:
  created:
    - tests/chapters/folder-grouping.test.ts
    - tests/kb/chapter-tree-merge.test.ts
  modified:
    - lib/chapters/actions.ts
    - lib/kb/tree.ts
    - "app/studio/[workId]/chapters/new/actions.ts"
    - "app/studio/[workId]/chapters/new/page.tsx"

key-decisions:
  - "Adapted folder-grouping.test.ts to use lib/kb/actions.ts's actual createNode export instead of the plan's non-existent createFolder helper (Rule 3 blocking-issue fix)"

patterns-established:
  - "assertChapterFolder mirrors assertWorkOwnership's shape: a private async helper returning boolean, called before any mutating query, with a fixed friendly Korean error string"

requirements-completed: [KB-05]

# Metrics
duration: 20min
completed: 2026-08-31
---

# Phase 04.1 Plan 03: Chapter/회차-Folder Grouping Data Layer Summary

**Validated `chapters.folder_id` write path (D-05 cross-work/cross-category rejection) plus a pure `groupChaptersByFolder` merge helper, so Plan 04.1-05's tree UI can render chapter leaves under 회차 folders without any further data-layer work.**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-08-31T10:16:25Z
- **Tasks:** 2 completed
- **Files modified:** 6 (4 modified, 2 created)

## Accomplishments
- `createChapter`/`listChapters` extended with a server-validated `folder_id` (D-05): null = 회차 root, a real 회차-category folder id in the same work succeeds, anything else (wrong category, wrong work) is rejected with the exact Korean error copy before any insert happens
- `groupChaptersByFolder` (pure, `lib/kb/tree.ts`) proven correct across all 4 spec cases: root-bucket, real-folder-bucket, dropped-null-root, empty-input
- `/studio/[workId]/chapters/new` now accepts an optional `folderId` search param (Promise-based `searchParams`, matching this Next.js version's convention) and threads it through `submitCreateChapter` → `createChapter`
- Integration test proves deleting a 회차 sub-folder (via existing `lib/kb/actions.ts`'s `deleteNode` → `soft_delete_kb_node` RPC from Plan 04.1-01) nulls the chapter's `folder_id` rather than orphaning or deleting the chapter (D-06 cascade-safety)

## Task Commits

Each task was committed atomically:

1. **Task 1: assertChapterFolder + createChapter/listChapters folder_id + groupChaptersByFolder** - `9364b76` (feat)
2. **Task 2: /chapters/new accepts folderId + folder-grouping and tree-merge tests** - `ad3c775` (feat)

**Plan metadata:** (pending) — added in this session's final commit

## Files Created/Modified
- `lib/chapters/actions.ts` - adds `assertChapterFolder`, `ERROR_INVALID_FOLDER`, `folderId`-aware `createChapterSchema`/`createChapter`, `folder_id` added to `listChapters`'s select
- `lib/kb/tree.ts` - adds `ChapterLeaf` interface and `groupChaptersByFolder` pure function
- `app/studio/[workId]/chapters/new/actions.ts` - `submitCreateChapter` accepts optional `folderId`, passed through to `createChapter`
- `app/studio/[workId]/chapters/new/page.tsx` - reads an optional `folderId` search param (Promise-based `searchParams`) and threads it into `submit()`
- `tests/chapters/folder-grouping.test.ts` - integration proof of D-05 validation + D-06 cascade-null (new)
- `tests/kb/chapter-tree-merge.test.ts` - unit proof of `groupChaptersByFolder`'s 4 spec cases (new)

## Decisions Made
- Used `lib/kb/actions.ts`'s existing `createNode(supabase, { ownerId, workId, parentId, category, nodeType, name })` export (with `category: '회차', nodeType: 'folder'`) in place of the plan's `createFolder(supabase, { ownerId, workId, scope, parentId, name })`, which does not exist in this codebase — see Deviations below.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `createFolder` does not exist in `lib/kb/actions.ts`; used `createNode` instead**
- **Found during:** Task 2 (writing `tests/chapters/folder-grouping.test.ts`)
- **Issue:** The plan's test code imports `createFolder` from `lib/kb/actions.ts` and calls it as `createFolder(admin, { ownerId, workId, scope: 'work', parentId, name })`. `lib/kb/actions.ts` (as landed by Plan 04.1-02, which owns folder-creation UI/actions) has no `createFolder` export — the actual folder/file creation function is `createNode(supabase, { ownerId, workId, parentId, category, nodeType, name, templateOverrideContent? })`, which always inserts `scope: 'work'` internally (no `scope` param) and requires an explicit `category`/`nodeType`.
- **Fix:** Replaced both `createFolder(...)` calls in `tests/chapters/folder-grouping.test.ts` with `createNode(admin, { ownerId: owner.id, workId, parentId: chapterRootId, category: '회차', nodeType: 'folder', name: '...' })`. No production code (`lib/chapters/actions.ts`, `lib/kb/tree.ts`) was affected — this was purely a test-authoring correction to match the real API surface.
- **Files modified:** tests/chapters/folder-grouping.test.ts
- **Verification:** `npx vitest run tests/chapters/folder-grouping.test.ts tests/kb/chapter-tree-merge.test.ts` exits 0 (8/8 tests pass, including both call sites of the replaced helper)
- **Committed in:** ad3c775 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Test-only correction to match the actual `lib/kb/actions.ts` API surface (owned by a sibling wave-2 plan, 04.1-02). No production-code scope creep; all planned behavior (D-05 validation, D-06 cascade-null, groupChaptersByFolder's 4 cases) proven exactly as specified.

## Issues Encountered
- `npx tsc --noEmit` reports a pre-existing `app/layout.tsx(21,50): error TS2304: Cannot find name 'LayoutProps'` unrelated to this plan (file not in this plan's `files_modified`, unchanged since a prior `refactor(ai-panel)` commit). Logged to `.planning/phases/04.1-kb/deferred-items.md`, not fixed (out of scope per plan execution rules).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Plan 04.1-05's tree UI can call `groupChaptersByFolder` directly against `listChapters`'s now-`folder_id`-inclusive output — no further data-layer work needed for KB-05.
- `/chapters/new?folderId=<uuid>` is ready for a "새 회차 추가" action inside a 회차 sub-folder's context menu (Plan 04.1-05 UI work).

---
*Phase: 04.1-kb*
*Completed: 2026-08-31*

## Self-Check: PASSED

All 7 created/modified files confirmed present on disk; both task commits (`9364b76`, `ad3c775`) confirmed in `git log`.
