---
phase: 04.1-kb
plan: 01
subsystem: database
tags: [postgres, supabase, plpgsql, kb_nodes, chapters, rls, triggers]

# Dependency graph
requires:
  - phase: 02-studio-core-writer-loop-no-ai
    provides: kb_nodes/chapters schema, create_work/ensure_account_template_root/soft_delete_kb_node functions, guard_locked_kb_node trigger pattern
provides:
  - kb_nodes.category CHECK widened to accept '회차' and 'custom'
  - All 7 structural folders (회차 + template/인물/장소/사건/세력/아이템) seeded locked in create_work, fixing Phase 2's shipped lock regression
  - Backfill of a locked 회차 folder for every pre-existing work (D-06)
  - chapters.folder_id nullable FK (+ index) for chapter/folder grouping (D-05)
  - guard_chapter_folder_no_kb_files trigger blocking generic KB file nodes under any 회차-category folder (D-04), while still allowing sub-folders
  - soft_delete_kb_node nulls chapters.folder_id before soft-deleting a referenced 회차 sub-folder (no orphaned/deleted chapters)
affects: [04.1-02, 04.1-03, 04.1-04, 04.1-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Idempotent migrations: drop constraint/trigger if exists, create or replace, add column if not exists, insert ... where not exists — safe to re-run against a live DB"
    - "Cascade-safety via security-definer RPC ordering: null out dependent FK columns before the soft-delete UPDATE, all inside one function, so ownership-scoped callers never orphan rows"

key-files:
  created:
    - supabase/migrations/0004_kb_custom_folders.sql
    - tests/kb/chapter-folder-guard.test.ts
  modified:
    - tests/studio/schema-smoke.test.ts

key-decisions:
  - "Chapter/folder grouping uses a nullable chapters.folder_id FK (not a join table) — folder_id IS NULL means 'at 회차 root', a state every existing chapter already satisfies with zero data migration"
  - "Guard trigger only blocks node_type='file' inserts/updates under a 회차-category parent — node_type='folder' rows (for grouping episodes into arcs) are explicitly allowed"

patterns-established:
  - "Locked-folder regression fix: re-lock via UPDATE + always insert is_locked=true in the seeding loop, rather than branching per-category, to prevent future forgetting"

requirements-completed: [KB-03, KB-05]

# Metrics
duration: 25min
completed: 2026-08-31
---

# Phase 04.1 Plan 01: Schema Foundation for KB Custom Folders Summary

**Migration `0004_kb_custom_folders.sql` widens `kb_nodes.category` for 회차/custom folders, fixes Phase 2's shipped lock regression so all 7 structural folders are locked, seeds/backfills a 회차 folder per work, adds `chapters.folder_id`, and adds a guard trigger keeping generic KB documents out of 회차.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-08-31T09:43:00Z (approx, worktree sync + npm install included)
- **Completed:** 2026-08-31T10:08:01Z
- **Tasks:** 2
- **Files modified:** 3 (1 created migration, 1 created test, 1 modified test)

## Accomplishments
- Live Supabase schema now has 7 locked structural folders per work (회차 added, and Phase 2's lock-regression on 인물/장소/사건/세력/아이템 fixed)
- Every pre-existing work backfilled with a locked 회차 folder — no orphaned/missing chapters going forward
- `chapters.folder_id` (nullable FK + partial index) live for chapter/folder grouping, ready for Wave 2/3 UI work
- `guard_chapter_folder_no_kb_files` trigger proven to reject generic KB file nodes under 회차 while still allowing sub-folders for organizing episodes
- `soft_delete_kb_node` now cascade-safe: deleting a 회차 sub-folder nulls the `folder_id` on any chapters that referenced it instead of orphaning them

## Task Commits

Each task was committed atomically:

1. **Task 1: Schema migration — widen category, fix lock regression, seed/backfill 회차, chapters.folder_id, guard trigger** - `1ca38f1` (feat)
2. **Task 2: Update schema-smoke assertions + new chapter-folder-guard integration test** - `a29f7fd` (test)

_Note: This plan applies a live schema migration as Task 1 (not TDD RED/GREEN); Task 2 is a standard test-authoring task verifying the already-applied migration._

## Files Created/Modified
- `supabase/migrations/0004_kb_custom_folders.sql` - Widens category CHECK, fixes lock regression, seeds/backfills 회차, adds chapters.folder_id + guard trigger, cascade-safe soft_delete_kb_node
- `tests/studio/schema-smoke.test.ts` - Updated to assert 7 locked parent-less kb_nodes (was 6/template-only); flipped the 인물-rename test to expect rejection; added a legacy-work backfill test
- `tests/kb/chapter-folder-guard.test.ts` - New: proves the guard trigger rejects KB file nodes under 회차 but allows sub-folders

## Decisions Made
- Followed the plan's Claude's-discretion resolutions verbatim: `chapters.folder_id` nullable FK over a join table (D-05), and the guard trigger scoped to `node_type='file'` only (D-04) — no deviation from the plan's exact SQL.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Worktree was stale relative to master; missing dependencies and env file**
- **Found during:** Pre-Task-1 setup
- **Issue:** This worktree's branch was ~150 commits behind `master` (last synced before Phase 3/4/04.1 work landed), so `.planning/phases/04.1-kb/` and most of the current codebase (AI panel, reader, etc.) didn't exist locally. Additionally `node_modules/` was absent and `.env.local` (gitignored, holds live Supabase credentials) was missing from the worktree.
- **Fix:** Fast-forward merged `worktree-agent-a98bb6b3f3f28b66c` onto `master` (`git merge master --ff-only`, safe since the worktree branch was a pure ancestor of master with a clean working tree), ran `npm install`, and copied `.env.local` from the main repo checkout into the worktree.
- **Files modified:** None tracked by git (`.env.local` is gitignored; `node_modules/` is gitignored); branch pointer moved via fast-forward merge.
- **Verification:** `.planning/phases/04.1-kb/04.1-01-PLAN.md` present after merge; `npx vitest run` executes successfully against the live DB afterward.
- **Committed in:** N/A (fast-forward merge, no new commit created; matches master's existing commit `7982325`)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary to even locate/execute the assigned plan file; no scope creep — no plan-file content was altered.

## Issues Encountered
None beyond the environment-sync deviation above. Migration applied cleanly on first try; idempotency re-run and all 49 tests in `tests/kb/`, `tests/studio/`, `tests/chapters/` pass with no regressions.

## User Setup Required
None - no new external service configuration required. (Existing `SUPABASE_DB_URL`/service-role credentials from `.env.local` were reused, already configured in the main checkout.)

## Next Phase Readiness
- Schema foundation fully live: category widening, lock-regression fix, 회차 seed/backfill, `chapters.folder_id`, guard trigger, and cascade-safe delete are all proven against the live Supabase project.
- Plans 04.1-02 through 04.1-05 (folder-creation UI, chapter-folder grouping UI, account-shared folder restructuring, cross-scope mentions) are now unblocked.
- No blockers or concerns carried forward from this plan.

---
*Phase: 04.1-kb*
*Completed: 2026-08-31*

## Self-Check: PASSED

- FOUND: supabase/migrations/0004_kb_custom_folders.sql
- FOUND: tests/kb/chapter-folder-guard.test.ts
- FOUND: .planning/phases/04.1-kb/04.1-01-SUMMARY.md
- FOUND commit: 1ca38f1
- FOUND commit: a29f7fd
