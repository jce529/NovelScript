---
phase: 04.1-kb
plan: 04
subsystem: ai
tags: [mentions, supabase, kb, gemini, chat, next-typegen]

# Dependency graph
requires:
  - phase: 04.1-01
    provides: kb_nodes.scope column, account_template scope semantics, custom category CHECK widening (0004_kb_custom_folders.sql)
provides:
  - Cross-scope @-mention search (work-scoped + account-shared documents, one-directional per owner)
  - getMentionedNodesContent resolving account-shared node ids so shared-doc content actually reaches the AI prompt
  - MentionAutocomplete scope-aware trailing text ("계정 공유" / "사용자 폴더") per UI-SPEC Copywriting Contract
affects: [04.1-05 (account-shared folder sidebar UI will produce the account_template node ids this plan's search/resolve pipeline already supports)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Two-query-then-merge for cross-scope Supabase reads (no cross-predicate UNION in supabase-js) — both queries owner_id-scoped to preserve one-directional D-10 guarantee"
    - "next typegen run once per fresh worktree/checkout to regenerate .next/types (gitignored) before npx tsc --noEmit will pass on files using Next.js 16's generated route prop types (e.g. LayoutProps<'/'>)"

key-files:
  created: []
  modified:
    - lib/ai/mentions.ts
    - tests/ai/mention-search.test.ts
    - "app/studio/[workId]/chapters/[chapterId]/ai-panel/MentionAutocomplete.tsx"

key-decisions:
  - "Widened MentionCandidate/MentionedDoc category type to KbCategory | 'custom' | 'template' and added optional/required scope field, kept QuickAddDialog/AiPanel call sites structurally compatible by making scope optional client-side"
  - "getMentionedNodesContent fix folded into the same plan as the search extension rather than deferred, since KB-04 isn't done until a mentioned shared doc's content actually reaches the prompt"

patterns-established:
  - "mentionTrailingText() single source of truth for UI-SPEC copywriting — never render raw 'custom'/'template' category strings to the writer"

requirements-completed: [KB-04]

# Metrics
duration: ~20min
completed: 2026-08-31
---

# Phase 04.1 Plan 04: Cross-scope mention search Summary

**Cross-scope @-mention search surfaces account-shared KB documents alongside work-scoped ones, fixes a real prompt-composition gap where shared-doc content silently vanished, and swaps the raw category label for UI-SPEC-compliant "계정 공유"/"사용자 폴더" trailing text in the autocomplete row.**

## Performance

- **Duration:** ~20 min (worktree sync/env setup + 2 tasks + human-verify checkpoint)
- **Started:** 2026-08-31T19:13:00+09:00
- **Completed:** 2026-08-31T19:30:00+09:00 (approx, after checkpoint approval)
- **Tasks:** 3 (2 auto + 1 checkpoint:human-verify)
- **Files modified:** 3

## Accomplishments
- `searchMentionNodes` now UNIONs the work's own file nodes (5 fixed categories + `custom`) with every file node in the caller's own `account_template`-scoped space (any category), never leaking another owner's shared docs
- `getMentionedNodesContent` fixed to resolve account-shared node ids (`work_id IS NULL`) in addition to work-scoped ones — previously these silently dropped out of AI prompt composition even after a successful mention-search match
- `MentionAutocomplete.tsx` result rows now show `계정 공유` for account-shared documents and `사용자 폴더` for work-scoped custom-category documents, never the raw internal category sentinel
- Live-verified end-to-end in the browser: seeded shared doc → `@` search surfaced it with the correct label → mentioned → AI chat reply correctly reflected the shared doc's content

## Task Commits

Each task was committed atomically:

1. **Task 1: Cross-scope searchMentionNodes + fix getMentionedNodesContent** - `6f3fc80` (feat, tdd)
2. **Task 2: MentionAutocomplete scope-aware trailing text** - `915565b` (feat)

Task 3 was a `checkpoint:human-verify` gate (no code changes) — approved by the user after live browser verification.

## Files Created/Modified
- `lib/ai/mentions.ts` - `searchMentionNodes`/`getMentionedNodesContent` made cross-scope-aware (two-query-then-merge, owner_id-scoped)
- `tests/ai/mention-search.test.ts` - added a new describe block (4 new `it`s) proving cross-scope search, ownership isolation, and mixed work+shared id content resolution; all 6 pre-existing assertions kept passing unmodified
- `app/studio/[workId]/chapters/[chapterId]/ai-panel/MentionAutocomplete.tsx` - widened local `MentionCandidate`, added `mentionTrailingText` helper, fallback `Folder` icon for non-fixed categories, `QuickAddDialog` call site tagged `scope: 'work'`

## Decisions Made
- Kept `MentionAutocomplete.tsx`'s locally-defined `MentionCandidate` interface (not importing the server-module one from `lib/ai/mentions.ts`) per the existing project pattern that avoids pulling `node:fs`-touching server code into the client bundle (STATE.md Phase 04-06 note)
- Made `scope` optional on the client-side `MentionCandidate` type so `QuickAddDialog`'s created-node result and `AiPanel.tsx`'s saved-proposal mention path stay structurally compatible without needing a scope-aware rewrite at each individual call site — both of those paths are always work-scoped/non-custom, so the fallback (raw category) branch is correct for them too

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fast-forwarded stale worktree branch onto master before starting**
- **Found during:** Task load / environment setup
- **Issue:** This worktree's branch (`worktree-agent-a12e13cf229eb335e`) was checked out at `d6c1a84`, missing Plan 04.1-01's schema migration and the entire `.planning/phases/04.1-kb/` directory (including this plan's own PLAN.md) — `master` and the sibling worktrees were already at `5decbfb`.
- **Fix:** `git merge --ff-only master` (clean fast-forward, no conflicts since the worktree had zero local commits beyond the stale point)
- **Files modified:** none beyond the merge itself (126 files brought in, matching master exactly)
- **Verification:** `git log --all --oneline` confirmed all worktrees converged on `5decbfb` as a common ancestor
- **Committed in:** N/A (fast-forward, no new commit)

**2. [Rule 3 - Blocking] Missing local dev environment (node_modules, .env.local)**
- **Found during:** Environment setup
- **Issue:** Fresh worktree had no `node_modules` and no `.env.local`
- **Fix:** `npm install`; wrote `.env.local` with the same Supabase/Gemini credentials used by the main checkout (gitignored, not committed)
- **Files modified:** none tracked (both gitignored)
- **Committed in:** N/A

**3. [Rule 3 - Blocking] `npx tsc --noEmit` failed on unrelated `app/layout.tsx` due to missing generated Next.js route types**
- **Found during:** Task 2 acceptance-criteria verification
- **Issue:** `LayoutProps<"/">` (Next.js 16's auto-generated route-prop type) doesn't exist until `.next/types` is generated; this fresh worktree had never run `next dev`/`next build`, so `.next/` didn't exist at all — unrelated to this plan's file changes but blocked the required `tsc` check from exiting 0
- **Fix:** Ran `npx next typegen` once to regenerate `.next/types` (gitignored, not committed)
- **Files modified:** none tracked (`.next/` is gitignored)
- **Verification:** `npx tsc --noEmit` exits 0 afterward
- **Committed in:** N/A

**4. [Rule 3 - Blocking] Port 3000 occupied by a sibling parallel-executor worktree's dev server**
- **Found during:** Pre-checkpoint automation for Task 3's manual verification
- **Issue:** `npm run dev -- --port 3000` failed with `EADDRINUSE` since another of the three parallel agents (04.1-02/04.1-03) already had a dev server on 3000
- **Fix:** Started the dev server on port 3011 instead
- **Files modified:** none
- **Verification:** `fetch('http://localhost:3011')` returned 200
- **Committed in:** N/A

---

**Total deviations:** 4 auto-fixed, all Rule 3 (blocking/environment setup), zero touching plan-scoped application logic.
**Impact on plan:** All four were prerequisites for being able to execute/verify the plan at all in a fresh, stale worktree running alongside sibling agents. No scope creep into application code beyond what the plan specified.

## Issues Encountered
- Could not automatically seed the checkpoint's required account-shared test document: the admin Supabase client's `auth.users` lookups (both a full user list and a single-email-filtered lookup) were blocked by the Claude Code permission classifier as sensitive production user-data queries. Resolved by providing the human a self-contained SQL snippet (using their own login email) to paste into Supabase Studio's SQL editor — a UI action, not a CLI command — which they ran successfully before verifying.

## User Setup Required

None - no external service configuration required beyond the one-time manual SQL seed described above, which the user has already completed and verified against.

## Next Phase Readiness

- KB-04 fully closed end-to-end: search, prompt-composition content resolution, and UI copy are all cross-scope-aware and live-verified against a real Gemini call.
- Plan 04.1-05 (account-shared folder sidebar UI) can now build directly on top of this — once it lets writers create `account_template`-scope nodes through the UI, this plan's `searchMentionNodes`/`getMentionedNodesContent` pipeline picks them up with zero further changes.
- No blockers.

---
*Phase: 04.1-kb*
*Completed: 2026-08-31*

## Self-Check: PASSED

- FOUND: lib/ai/mentions.ts
- FOUND: tests/ai/mention-search.test.ts
- FOUND: app/studio/[workId]/chapters/[chapterId]/ai-panel/MentionAutocomplete.tsx
- FOUND: .planning/phases/04.1-kb/04.1-04-SUMMARY.md
- FOUND commit: 6f3fc80
- FOUND commit: 915565b
