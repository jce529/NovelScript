---
phase: quick-260919-w1m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ai/cost.ts
  - lib/ai/chat.ts
  - tests/ai/cost-estimate.test.ts
  - tests/helpers/mock-provider.ts
  - tests/ai/chat-idempotency.test.ts
  - tests/ai/chat-refusal.test.ts
autonomous: true
requirements: [BUG-04]

must_haves:
  truths:
    - "사고 토큰(usage.thoughtsTokens)이 보고되면 출력 단가로 지갑 차감액에 합산된다"
    - "thoughtsTokens가 null(미보고)이면 0으로 취급되어 기존 차감액과 동일하다"
    - "거절(refusal) 응답도 같은 합산(입력 + 출력 + 사고)으로 차감된다"
    - "차감액은 여전히 호출 전 잔액(walletBalance)으로 clamp되고, 같은 멱등 키로 한 번만 차감된다"
    - "사고 토큰은 별도 필드로 노출되지 않는다 — refusal.debitAmount 등 결과에는 합계만 담긴다"
  artifacts:
    - path: "lib/ai/cost.ts"
      provides: "computeDebitAmount의 thoughtsTokenCount 합산"
      contains: "thoughtsTokenCount"
    - path: "lib/ai/chat.ts"
      provides: "chat()이 result.usage.thoughtsTokens를 computeDebitAmount에 전달"
      contains: "thoughtsTokenCount: result.usage.thoughtsTokens"
  key_links:
    - from: "lib/ai/chat.ts"
      to: "lib/ai/cost.ts computeDebitAmount"
      via: "단일 debitAmount 계산 (완료·거절 경로 공용)"
      pattern: "thoughtsTokenCount: result\\.usage\\.thoughtsTokens"
---

<objective>
BUG-04 수정: Gemini 사고(thinking) 토큰을 출력 단가로 지갑 차감에 포함한다 (09-CONTEXT D-12, BUG-04 A안).

Purpose: Google은 사고 토큰을 출력 요금으로 청구하는데 현재 차감은 input+output만 계산해 플랫폼이 원가를 손해본다(08-09 실측 호출당 ~245 사고 토큰). Phase 9는 이 수정 위에 OpenAI/Anthropic reasoning 토큰을 같은 규칙으로 확장한다(D-13).

Scope (D-13, 잠금): `computeDebitAmount`의 사고 토큰 합산 + 거절 차감 경로 + 관련 테스트만.
Out of scope: Gemini `thinkingConfig` 최소화(D-11, Phase 9), 모델/제공자별 단가 테이블, `UsageReport.reported`에 thoughts 플래그 추가(불필요 — `thoughtsTokens: null`이 이미 "미보고"를 표현), UI 변경(D-12: 합계만 표시 — 현재 UI가 이미 합계 `debitAmount`만 쓰므로 변경 없음).

Output: 수정된 cost.ts / chat.ts, 사고 토큰 케이스가 추가된 테스트 3종.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-provider-adapter-idempotent-debit/bugs/BUG-04-thinking-tokens-not-debited.md
@.planning/phases/09-openai-anthropic-adapters-pricing/09-CONTEXT.md
@lib/ai/cost.ts
@lib/ai/chat.ts

실행 방식: Codex CLI에 TDD로 위임(codex-tdd-pipeline 스킬). 각 task는 반드시 (1) 실패하는 테스트 작성 → `npx vitest run <file>`로 RED 확인 → (2) 최소 구현 → (3) GREEN 확인 순서를 지킨다.

<interfaces>
From lib/ai/providers/types.ts (변경하지 않음):
```typescript
export interface UsageReport {
  inputTokens: number;
  outputTokens: number;
  thoughtsTokens: number | null;   // Gemini thoughtsTokenCount; null = 미보고
  reported: { input: boolean; output: boolean };
}
```

From lib/ai/cost.ts (현재):
```typescript
export interface ComputeDebitInput {
  modelTier: ModelTier;
  promptTokenCount: number;
  candidatesTokenCount: number;
}
export function computeDebitAmount({ modelTier, promptTokenCount, candidatesTokenCount }: ComputeDebitInput): number {
  const inputCost = promptTokenCount * walletTokensPerGeminiToken(modelTier, 'input');
  const outputCost = candidatesTokenCount * walletTokensPerGeminiToken(modelTier, 'output');
  return Math.max(0, Math.ceil(inputCost + outputCost));
}
// lite/pro 단가: input 1.50, output 9.00 USD/1M, USD_TO_KRW=1400, KRW_PER_WALLET_TOKEN=10
// => wallet/token: input 0.00021, output 0.00126
```

From lib/ai/chat.ts:183 (현재 — 완료·거절 경로가 공유하는 유일한 차감 계산):
```typescript
const debitAmount = Math.min(walletBalance, computeDebitAmount({
  modelTier: input.modelTier, promptTokenCount: result.usage.inputTokens, candidatesTokenCount: result.usage.outputTokens,
}));
// ...이후 apply_wallet_delta(p_delta: 0 - debitAmount), 그리고 if (result.refusal) { ... refusal: { ..., debitAmount } }
```

From tests/helpers/mock-provider.ts:
```typescript
export function okResult(text = '[REPLY]\n(mock) 응답', inputTokens = 10, outputTokens = 10): GenerateResult
// usage.thoughtsTokens는 항상 null
```

From tests/ai/chat-refusal.test.ts:
```typescript
function refusal(stage, reasonCode, inputTokens, outputTokens, reported = true): GenerateResult
// usage.thoughtsTokens는 항상 null
```

computeDebitAmount 호출부 전체 (grep 확인됨): lib/ai/chat.ts:183 (프로덕션 유일), tests/ai/cost-estimate.test.ts, tests/ai/chat-refusal.test.ts, tests/ai/chat-idempotency.test.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: computeDebitAmount에 사고 토큰을 출력 단가로 합산</name>
  <files>tests/ai/cost-estimate.test.ts, lib/ai/cost.ts</files>
  <behavior>
    - lite, prompt 1000 / candidates 2048 / thoughts 2000 → 6  (0.21 + 2.58048 + 2.52 = 5.31048 → ceil 6)
    - lite, prompt 1000 / candidates 2048 / thoughts 생략 → 3 (기존 테스트 그대로 통과 — 하위 호환)
    - lite, prompt 1000 / candidates 2048 / thoughts null → 3 (null = 0)
    - lite, prompt 0 / candidates 0 / thoughts 1000 → 2 (1.26 → ceil 2; 사고 토큰은 input이 아니라 OUTPUT 단가)
    - 결과는 여전히 0 이상 정수, -0이 아님: prompt 0 / candidates 0 / thoughts 0 → 0 이고 Object.is(result, -0) === false
  </behavior>
  <action>
    RED: tests/ai/cost-estimate.test.ts의 기존 describe 안에 위 behavior 5개를 `it(...)`으로 추가한다(기존 `debits the ACTUAL post-call usage` 테스트는 수정하지 않는다). `npx vitest run tests/ai/cost-estimate.test.ts` 실행 — thoughtsTokenCount가 아직 타입/계산에 없으므로 6·2 케이스가 실패해야 한다.

    GREEN (per D-12): lib/ai/cost.ts에서
    - `ComputeDebitInput`에 선택 필드 `thoughtsTokenCount?: number | null;` 추가 (선택형이라 기존 호출부 컴파일 유지).
    - `computeDebitAmount`에서 `const thoughts = thoughtsTokenCount ?? 0;` 후 `const outputCost = (candidatesTokenCount + thoughts) * walletTokensPerGeminiToken(modelTier, 'output');` 로 출력 단가에 합산. `Math.max(0, Math.ceil(...))` 반올림/하한 로직은 그대로.
    - JSDoc에 "사고(thinking) 토큰은 제공자가 출력 요금으로 청구하므로 출력 단가로 합산 (BUG-04, 09-CONTEXT D-12). null/미지정 = 0" 한 줄 추가.
    - 별도 thoughts 단가·상수를 만들지 않는다 (D-12: 출력 단가 그대로). `computeMaxOutputTokens`는 건드리지 않는다.
  </action>
  <verify>
    <automated>npx vitest run tests/ai/cost-estimate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "thoughtsTokenCount" lib/ai/cost.ts` 가 인터페이스 필드와 함수 본문 양쪽에서 매치
    - `grep -n "candidatesTokenCount + thoughts" lib/ai/cost.ts` 매치 (출력 단가에 합산)
    - tests/ai/cost-estimate.test.ts에 `thoughtsTokenCount: 2000` 과 `toBe(6)` 이 존재
    - `npx vitest run tests/ai/cost-estimate.test.ts` 전부 통과
  </acceptance_criteria>
  <done>computeDebitAmount가 사고 토큰을 출력 단가로 합산하고, 미지정/null이면 기존 값과 동일하다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chat() 차감(완료·거절 공용 경로)에 usage.thoughtsTokens 전달 + 테스트</name>
  <files>tests/helpers/mock-provider.ts, tests/ai/chat-idempotency.test.ts, tests/ai/chat-refusal.test.ts, lib/ai/chat.ts</files>
  <behavior>
    - 완료 경로: okResult('[REPLY]\nx', 12345, 6789, 5000) → p_delta === -computeDebitAmount({lite, 12345, 6789, thoughtsTokenCount: 5000}) === -18 (사고 미포함 값 12와 달라야 함)
    - 잔액 clamp 유지: 잔액 5, okResult('[REPLY]\n응답', 1000, 1000, 10000) (사고 포함 차감 15 > 5) → p_delta === -5, 잔액 0, status 'completed'
    - 멱등 유지: 위 사고 포함 결과로 같은 키 재호출 → status 'already_processed', 차감 row 1개
    - 거절 input 단계: refusal('input','SAFETY', 900, 0, true, 1000) → p_delta === -2 (= computeDebitAmount({lite, 900, 0, thoughtsTokenCount: 1000}); 사고 미포함이면 1), result.refusal.debitAmount === 2
    - 거절 output 단계: refusal('output','RECITATION', 3000, 4000, true, 2000) → p_delta/ debitAmount 가 thoughtsTokenCount 2000 포함 값과 일치
    - 결과 객체에 사고 토큰 별도 노출 없음: JSON.stringify(result) 에 'thoughts' 문자열이 포함되지 않음 (D-12 합계만)
  </behavior>
  <action>
    RED:
    1. tests/helpers/mock-provider.ts `okResult`에 4번째 선택 인자 `thoughtsTokens: number | null = null` 추가하고 `usage.thoughtsTokens`에 넣는다 (기본값 null이라 기존 호출부 영향 없음).
    2. tests/ai/chat-refusal.test.ts의 로컬 `refusal()` 헬퍼에 6번째 선택 인자 `thoughtsTokens: number | null = null` 추가해 usage에 반영.
    3. tests/ai/chat-idempotency.test.ts에 behavior의 완료 경로 3개(사고 포함 차감, 잔액 clamp, 멱등 재호출) `it(...)` 추가. 기대값은 `computeDebitAmount({ ..., thoughtsTokenCount })`로 계산하되, 최소 한 곳은 리터럴(-18, -5)로도 단언해 합산이 실제로 반영됐는지 고정한다.
    4. tests/ai/chat-refusal.test.ts에 behavior의 거절 케이스 2개 + 'thoughts' 비노출 단언 추가 (리터럴 2도 단언).
    5. `npx vitest run tests/ai/chat-idempotency.test.ts tests/ai/chat-refusal.test.ts` — 새 케이스가 실패(사고 미반영)하는지 확인. 기존 테스트는 계속 통과해야 한다.

    GREEN (per D-12 — 거절 경로도 같은 합산): lib/ai/chat.ts:183의 `computeDebitAmount({...})` 인자에 `thoughtsTokenCount: result.usage.thoughtsTokens` 추가. 이 한 줄이 완료·거절 양쪽 경로의 유일한 차감 계산이므로 거절 분기(`if (result.refusal)`)에 별도 계산을 만들지 않는다. `Math.min(walletBalance, ...)` clamp, pre-debit recheck, apply_wallet_delta 호출, 멱등 키 로직은 절대 변경하지 않는다. 바로 위 주석에 "사고 토큰 포함(BUG-04/D-12)"을 짧게 덧붙인다.

    마지막으로 전체 스위트 `npm test` 실행해 회귀 없음 확인 (tests/ai/chat.test.ts, tests/ai/provider-*.test.ts, tests/admin/sanctions.test.ts 는 thoughtsTokens: null 이라 값 변화 없어야 함). 코드 수정 후 `graphify update .` 실행 (AGENTS.md 규칙).
  </action>
  <verify>
    <automated>npx vitest run tests/ai/chat-idempotency.test.ts tests/ai/chat-refusal.test.ts tests/ai/cost-estimate.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "thoughtsTokenCount: result.usage.thoughtsTokens" lib/ai/chat.ts` 정확히 1건
    - `grep -c "computeDebitAmount(" lib/ai/chat.ts` 가 1 (거절용 별도 계산 없음)
    - `grep -n "Math.min(walletBalance, computeDebitAmount" lib/ai/chat.ts` 여전히 매치 (clamp 유지)
    - tests/ai/chat-idempotency.test.ts 와 tests/ai/chat-refusal.test.ts 에 `thoughtsTokenCount` 사용 케이스 존재
    - `npm test` 전체 통과
  </acceptance_criteria>
  <done>완료·거절 응답 모두 사고 토큰을 출력 단가로 포함해 차감되고, clamp·멱등 동작과 기존 테스트는 그대로 통과한다.</done>
</task>

</tasks>

<verification>
- `npm test` 전체 GREEN
- `npx tsc --noEmit` 타입 에러 없음 (optional 필드라 기존 호출부 영향 없어야 함)
- grep: lib/ai/chat.ts 의 유일한 computeDebitAmount 호출이 thoughtsTokens를 전달
- UI/문구 파일 변경 없음 (`git diff --stat` 에 app/ 하위 파일 없음) — D-12 합계만 표시
</verification>

<success_criteria>
- 사고 토큰 245개가 보고된 lite 호출은 그만큼(출력 단가) 더 차감된다
- thoughtsTokens null 인 기존 경로·테스트의 차감값은 변하지 않는다
- 거절 차감도 같은 합산 사용, 잔액 clamp·멱등 키 한 번 차감 보장 유지
- BUG-04 파일 status는 executor가 "fixed (quick 260919-w1m)"로 갱신
</success_criteria>

<output>
After completion, create `.planning/quick/260919-w1m-bug-04-computedebitamount/260919-w1m-SUMMARY.md`, and update `.planning/phases/08-provider-adapter-idempotent-debit/bugs/BUG-04-thinking-tokens-not-debited.md` frontmatter `status: fixed (quick 260919-w1m, D-12 A안)`.
</output>
